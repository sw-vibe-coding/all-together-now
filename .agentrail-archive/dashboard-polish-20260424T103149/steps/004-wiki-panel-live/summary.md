wiki_get_page honors If-None-Match → 304. Client polls the open panel every 5s with If-None-Match, re-renders + flashes on change, pauses on document.hidden + panel-close. Event-row wiki links reuse the panel when open; fall through to new-tab when closed. Deleted pages render '(page deleted)' muted. Live-verified 304 + flash re-render + 0 fetches-while-closed + cross-link reuse via Playwright.