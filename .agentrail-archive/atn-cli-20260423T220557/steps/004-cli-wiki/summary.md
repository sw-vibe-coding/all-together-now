Added wiki list/get/put/delete with ETag handling. list prints titles (one per line or JSON); get streams markdown body with --verbose surfacing ETag on stderr; put/delete accept --if-match. 409 conflicts exit 2 with the current ETag for script loops. Shared read_body helper for --file/--stdin selection. 4 new unit tests (18 total). Live-verified every path incl. 409, 404, usage errors.