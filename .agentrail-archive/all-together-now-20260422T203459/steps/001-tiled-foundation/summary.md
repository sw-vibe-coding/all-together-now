Tiled foundation shipped. Replaced heat-driven treemap with a simple coord-left (55%) + workers-right gridded layout. buildLayoutItems sorts by name; computeLayout returns {windows:[{id,x,y,w,h}]} directly; applyLayout places each window at its rect. pickTermScale lost the hysteresis param (no longer needed with stable sizes). layoutState grows layoutMode/sortMode fields (tiled/name this step; later saga steps add stack/carousel/recent). Refresh is now genuinely idempotent — no tile drift, no zoom oscillation. 60 tests green; clippy clean.