Window chrome shipped. Each agent header now has _ □ 📌 ▸ ↻ ✕ icon buttons wired to windowMinimize/windowMaximizeToggle/togglePin/toggleConfig/reconnectAgent/confirmDelete. layoutState.windows tracks per-agent state (normal/minimized/maximized + pinned) and layoutState.selectedId tracks the click-selected window. Clicking a header outlines the window in accent green; clicking chrome buttons fires the action; clicking inside xterm still routes keys to PTY as before. Minimized agents move off-screen and appear as 180x48 cells in a new #window-dock footer strip; clicking a cell restores. Maximize lifts one agent to 80% centered, others implicit-dock-minimized for the duration. computeLayout returns {windows, minimized}; applyLayout off-screens not-in-layout panels, applies .selected class, and calls renderWindowDock. 60 tests green; clippy clean.