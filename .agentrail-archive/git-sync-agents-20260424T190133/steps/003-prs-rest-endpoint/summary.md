atn-server gains /api/prs surface: prs.rs module with PrsState{prs_dir,central_repo,lock} + read_records (sort+skip-bad-json), read_record (id sandbox: rejects empty/slash/backslash/dotdot/null), write_record (tempfile+rename atomic), git_merge (non-zero stdout+stderr combined; merge --abort on failure for clean state). 4 routes: GET /api/prs (?status filter), GET /api/prs/{id} (404), POST /merge (success flips Merged + merge_commit=HEAD-SHA + merged_at RFC3339; 404; 409 not-open; 409 {error,stderr} on conflict), POST /reject (Rejected + rejected_at). CLI flags --prs-dir (default base/.atn/prs) + --central-repo (default base) via parse_server_args supporting positional config + --flag value + --flag=value. SharedState.prs serialized via Mutex<()>. 8 unit tests (sort/garbage/round-trip/404/sandbox/atomic-tmp/filter/empty-dir/git_merge real conflict on tempdir fixture). New prs_endpoints integration test boots atn-server with ATN_PORT=0 against tempdir non-bare central + clone, pushes 2 PR refs, drops 2 PrRecord JSONs, exercises list (filtered+unfiltered)/get/merge-success/merge-409-second-attempt/reject/?status=rejected/missing-id-404; central main confirmed has 'Merge refs/heads/pr/alice-feature' + feature.txt. cargo test workspace + clippy --all-targets -D warnings clean.