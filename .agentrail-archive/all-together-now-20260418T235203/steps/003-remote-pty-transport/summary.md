Remote PTY transport: OutputSignal::Disconnected emitted by reader on EOF, state tracker flips AgentState::Disconnected. tools/fake-mosh shell-script recorder + symlinks for ssh; three new integration tests (mosh argv, ssh argv, exit→Disconnected) all green. New /api/agents/{id}/reconnect endpoint hard-kills the local mosh/ssh child without Ctrl-C and respawns — re-attaches to remote tmux. DELETE now sends C-b :kill-session over PTY for remote agents before shutdown. docs/remote-pty.md captures the manual queenbee walkthrough; CI never touches mosh.