Surface the composed launch_command in the static dashboard agent header as a title/tooltip. API already returns launch_command in GET /api/agents (step 2). Users should be able to hover an agent name and see the actual shell command ATN typed into the PTY — including the full `mosh user@host -- tmux ...` string for remote agents. Self-documents the demo without opening the Config editor.