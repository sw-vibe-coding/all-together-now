New Agent dialog: structured SpawnSpec (name/role/transport/host/user/working_dir/project/agent/agent_args) in atn-core with 13 unit tests; POST /api/agents now consumes the spec, validates per-transport (400 with missing field list), composes commands (local vs mosh/ssh+tmux), stores specs alongside configs, and returns them via AgentInfo. Static HTML form and Yew modal both ship with live command preview. Live smoke test confirmed 400/201/GET/DELETE all work end-to-end.