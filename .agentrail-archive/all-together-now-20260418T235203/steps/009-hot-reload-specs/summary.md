Hot-reload watcher now keeps agent_specs in sync with agents.toml. External edits that add, modify, or remove [agent.spec] subtables propagate to state.agent_specs — the UI sees structured data immediately and save_config round-trips it cleanly. Live smoke: injected a spec via file write, saw /api/agents expose it after the 500ms debounce, then saved it back to disk as a clean [agent.spec] block.