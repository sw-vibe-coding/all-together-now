Three-agent demo ready end-to-end. ATN_PORT env (plus 'atn-server ready on <addr>' stdout marker) enables ephemeral-port tests. Three fake-CLI shims (fake-claude/fake-codex/fake-opencode-glm5), real-topology fixtures (mighty-mike/claude + queenbee/devh1/codex + queenbee/devr1/opencode-z-ai-glm-5), and setup.sh that boots or reuses a server and POSTs each fixture (ATN_DEMO_REAL=1 to swap in real CLIs). New integration test spawns the real atn-server binary, POSTs three local-transport variants using the fake CLIs, polls states, routes two coordinator→worker events, and asserts router delivery + /api/events surfaces them.