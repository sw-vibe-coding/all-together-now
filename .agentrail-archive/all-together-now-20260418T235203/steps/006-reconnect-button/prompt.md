Add a Reconnect button to the agent panel in the static HTML dashboard (crates/atn-server/static/index.html). Place it next to Restart. Click posts to /api/agents/{id}/reconnect (endpoint already exists from the remote-pty-transport step). Show transient "Reconnecting..." → "Reconnected" / "Failed" text like the Restart button does. This closes the UX gap from step 3: the endpoint exists but the demo story forced curl. Yew atn-ui is placeholder-level (per docs/status.md) so skip it there; static HTML is the visible UI.