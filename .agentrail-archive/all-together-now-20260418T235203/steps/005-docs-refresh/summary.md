Docs refreshed. usage.md rewritten around empty-start + New Agent dialog (SpawnSpec fields, per-transport command composition, lifecycle endpoints incl /reconnect, ATN_PORT + ready marker, updated REST table). New demo-three-agent.md walkthrough covers both the CI (fake shims + cargo test) and live (ATN_DEMO_REAL=1 to queenbee) paths, with ASCII dashboard sketch and reconnect-after-pkill-mosh section. uber-use-case.md Implementation Notes replaced with what shipped and gains See-also cross-links. status.md adds R1-R5 rows for the saga. README gets a quickstart and docs index. cargo doc --workspace --no-deps builds warning-free; 52 tests green.