Save-to-TOML now round-trips the full SpawnSpec. AgentEntry grows an optional spec subtable; on load, to_agent_config recomposes launch_command from the spec and startup populates state.agent_specs; on save_config, spec is written whenever known and the flat launch_command is blanked. Legacy entries continue to round-trip unchanged via skip_serializing_if. Two new unit tests and a live smoke confirm the format.