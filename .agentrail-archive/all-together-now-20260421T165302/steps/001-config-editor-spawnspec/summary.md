Config editor round-trips SpawnSpec. UpdateAgentBody gains optional spec; update_agent validates, recomposes launch_command, persists new spec and re-derived config, runs the existing shutdown+respawn. Legacy flat shape kept for backward compat. Dashboard swaps to a structured editor (role/transport/user/host/dir/project/agent/args + live preview + missing-field error) when agent.spec is present; legacy TOML-loaded agents still get the flat editor. applyConfig dispatches the right body per mode. Live smoke confirmed spec-shape PUT rewrites spec+launch, legacy PUT overrides launch without touching spec, invalid spec returns 400 with missing-fields. 60 tests green; clippy clean.