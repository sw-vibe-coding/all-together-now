New Agent dialog + Config editor spec mode now hide the user/@/host group entirely when transport=local — coordinator creates see just name/role/transport/dir/project/agent/args. readSpawnSpec and readSpecFromEditor force user/host to null on local so hidden-input residue cannot leak into the composed command. localStorage atn-last-host remembers the host on every successful mosh/ssh POST; pre-filled on the next dialog open when transport flips non-local (user/dir/agent stay blank — they differ per worker). Opening +New Agent focuses the name input for a keyboard-first demo flow. Live smoke confirmed spec.host=null spec.user=null for local coordinator; mosh worker composes correctly. 60 tests green; clippy clean.