Heat score foundation shipped. HeatState (time-aware EWMA of bytes/sec with ~30s half-life), state_boost multipliers, compute_score blending with floor+clamp, spawn_heat_tracker subscribed per agent, wired into every spawn path + delete + hot-reload. GET /api/agents/heat decays each entry to now before returning HeatInfo. 6 new unit tests; 60 workspace tests total green; clippy --all-targets clean. Live smoke confirmed rise-on-spawn, decay-on-silence, rise-on-activity, clean delete.