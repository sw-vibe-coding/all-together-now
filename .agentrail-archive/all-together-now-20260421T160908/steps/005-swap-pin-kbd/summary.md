Pin row + keyboard shortcuts + localStorage shipped. .pin-row strip renders mini compact tiles for user-pinned agents (fixed 180x82, sparkline + last-line + state, X to unpin); pinned ids excluded from treemap heat sizing and auto-pick. .btn-pin on every agent-header toggles pin state. Keyboard: 1-9 focus Nth hottest unpinned, 0 coordinator, f toggles focus width (medium 40-50% / large 55-70%), p pin/unpin current focus, / filter stub, Esc clears focus. isTypingTarget guard avoids key capture in inputs/xterm. localStorage atn-scale-ui-v1 persists { pins, focusId, focusSize } and hydrates at init before the first render. 60 tests green; clippy clean.