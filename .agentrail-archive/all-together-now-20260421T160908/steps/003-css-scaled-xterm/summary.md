CSS-scaled xterm shipped. Panel template nests .agent-terminal > .term-scale > xterm-host; initTerminal creates xterm at fixed 120x40 (TERM_NATIVE_COLS/ROWS); pickTermScale snaps to {0.25,0.33,0.5,0.66,0.8,1.0} or null→compact below threshold; updateTileScales walks tiles on resize/ResizeObserver/grid-class change and updates --term-k — no fitAddon.fit, no POST /resize. Only setFocus (dev-exposed) fits + syncPtySize for a single focused tile. Inline self-test covers 8 cases; runs on load and logs PASS. 60 workspace tests green; clippy clean.