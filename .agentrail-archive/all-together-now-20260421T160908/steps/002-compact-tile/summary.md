Compact tile shipped. .agent-compact CSS + createCompactTile builds a no-xterm tile with header + inline SVG sparkline + ANSI-scrubbed last-line + click-to-focus hint. SSE onmessage feeds recordOutputBytes into a 200-char per-agent last-line buffer; startHeatPolling polls /api/agents/heat at 1Hz and maintains a 60-sample bytes_per_sec ring per agent. Click dispatches swap-to-focus CustomEvent (step 4 will wire layout; default logs). window.toggleCompactMode(id) flips between xterm and compact for manual testing before step 4 auto-picks by tile size. 60 workspace tests green; clippy clean.