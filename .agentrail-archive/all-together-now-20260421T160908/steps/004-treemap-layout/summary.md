Squarified treemap replaces the cols-N grid. .dashboard.treemap-mode makes the dashboard an absolutely-positioned canvas; squarify (Bruls/Huijsen/van Wijk) lays tiles sized by per-agent smoothed heat; focus panel carves 40–55% of viewport width and scales with its relative heat. pollHeat feeds smoothedHeat (EWMA alpha 0.3, ~5s half-life). Refresh tick every 1.5s unless frozen; swap-to-focus click (header + compact) sets focusId, freezes for 5s, runs setFocus for PTY+xterm fit. Auto-pick prefers coordinator. ensureModeFor flips each tile between xterm and compact via step-3 pickTermScale. Inline self-tests for squarify; 60 workspace tests green; clippy --all-targets clean.