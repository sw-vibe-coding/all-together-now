Filter bar + group-by + saved layouts shipped. New #filter-bar between view-tabs and pin-row: /-focused text input, transport/role/state chips (OR within category, AND across), group-by select, Layouts dropdown (Save/Apply/Delete). matchesFilter fuzzy-matches across name/project/role/spec.host/spec.user/spec.agent; buildLayoutItems hides non-matching non-pinned tiles; pin cells get muted "(filtered)" suffix when excluded. groupItems + groupedSquarify partition the rest-rect along the longer axis by group heat and squarify each sub-rect; dashed .group-band overlay labels each cluster. pollStates now caches full AgentInfo on agents.entry.info. layoutState extended with filterText/filterChips/groupBy (persisted); separate atn-scale-ui-saved-layouts-v1 holds named snapshots. Filter-bar visibility tied to agent count. 60 tests green; clippy clean.