Implemented Phase 1: PTY integration tests (spawn_and_echo, ctrl_c_interrupt, shutdown_clean, transcript_logging, session_manager_lifecycle), TranscriptWriter module (raw output to transcript.log, structured events to events.jsonl), SessionManager (multi-agent lifecycle), PgmController trait (public API boundary). Fixed PtySession to retain master handle. 23 tests pass, zero clippy warnings.