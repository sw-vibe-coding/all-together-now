Phase 5: Message routing — outbox polling router, PushEvent routing (deliver to known agents via PTY inject + inbox, escalate unknown targets to wiki, broadcast untargeted to coordination log), append-only event log with REST API (GET /api/events?since=N, POST /api/events), Events tab in browser UI with escalation banners and auto-polling, 3 new routing unit tests, zero clippy warnings, all 32 tests pass