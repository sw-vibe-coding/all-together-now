Stood up crates/atn-cli with clap + ureq + serde. Base-URL resolves --base-url → ATN_URL → localhost:7500; exit codes 0/1/2/3/4 documented. First subcommands: agents list (table + json formats, right-padded table, '(no agents)' empty) and agents state <id> (404 → exit 2). 6 unit tests cover URL resolution, table formatter, state_label. Live smoke: empty list, seeded bash agent tabulates cleanly, unknown id exits 2.