atn-cli prs subcommand: PrsCommand{List/Show/Merge/Reject} dispatched via run_prs. List: GET /api/prs with optional ?status= filter, renders ID|AGENT|BRANCH→TARGET|STATUS|SUMMARY table (summary truncated to 80 chars char-counted with … ellipsis), '(no prs)' empty marker; --format json pretty-prints. Show: GET /api/prs/{id}, table mode prints key:value lines with merge_commit/merged_at/rejected_at/last_error included only when set; 404 → exit 2. Merge/Reject: POST /api/prs/{id}/{action}; on 409 report_pr_conflict parses {error,stderr} (conflict path) or {error,status} (not-open path) and surfaces the right field on stderr, exits 2 (wiki ETag mismatch convention). atn-core added as dep so PrRecord/PrStatus shapes are shared. 10 new unit tests (30 total): table cols/branch→target/truncation-79+ellipsis/empty-marker, pr_show open-omits-vs-merged-includes lifecycle fields, pr_status_str+PrStatusArg::as_str, truncate keep+cap, conflict-with-stderr/with-status/garbage-body all exit 2. Integration: new atn_cli_prs_round_trip + PrsServerGuard fixture (tempdir non-bare central + clone, push pr/alice-feature, drop PrRecord JSON, boot atn-server with --prs-dir/--central-repo) drives list/show/missing/merge (verifies central main log has 'Merge refs/heads/pr/alice-feature') / list-merged-json / second-merge-409 paths. cargo test workspace + clippy --all-targets -D warnings clean.