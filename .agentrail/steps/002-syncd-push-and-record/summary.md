atn-syncd handle_marker: parse_marker(key=value, # comments, defaults: branch via rev-parse, target=main, summary='<branch> ready for review'), git push <remote> <branch>:refs/heads/pr/<agent>-<branch> via Command, git rev-parse for SHA, write pretty PrRecord JSON to <prs-dir>/<id>.json (id=<agent>-<branch>-<short7>), rename marker to <marker>.queued.<short>; all-or-nothing — any failure leaves marker for retry. HandleOutcome surfaces branch/pr_id/short_sha/record_path/renamed_marker for the success log. 7 new unit tests on tempdir fixture (bare central.git + worktree with commit + central remote): success path asserts pushed ref + JSON shape + marker rename; branch/target overrides; push-failure preserves marker + writes no record. 13 tests in atn-syncd, workspace test + clippy -D warnings clean. Live smoke: pr/alice-main pushed, prs/alice-main-<sha>.json (status=open, created_at RFC3339) on disk, marker → .queued.<sha>.