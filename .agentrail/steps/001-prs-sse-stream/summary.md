/api/prs/stream SSE endpoint + notify-based filesystem watcher: new prs_stream.rs (PrsEvent snake-case union Snapshot/Created/Updated/Removed; PrsBroadcast tokio broadcast wrapper, capacity 256; spawn_watcher on dedicated thread with std::sync::mpsc → 50ms coalescing window so tempfile+rename writes don't fan out twice; translate_and_broadcast partitions touched vs removed paths, parses PrRecord, distinguishes Created vs Updated via per-watcher HashSet<id> seen). pr_stream handler subscribes to broadcast, emits Snapshot from spawn_blocking read_records, forwards every event with 15s keep-alive; Lagged(n) logged at warn, Closed ends stream. boot canonicalizes prs_dir BEFORE spawning watcher (kqueue is finicky about relative paths). Merge/Reject routes push PrsEvent::Updated directly so platforms that collapse rename events still surface state (clients de-dupe by id+status). 11 new unit tests + new prs_stream_pushes_a_create integration test (curl -sN, drop fresh PR JSON, assert snapshot + created arrive within 5s). Live-verified full lifecycle: snapshot → created → updated → removed all surface in real time. Side fix: 3 atn-cli base_url tests now share OnceLock<Mutex> env_lock() so concurrent unsafe env writes don't race in parallel runs.