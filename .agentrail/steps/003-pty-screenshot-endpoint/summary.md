Added GET /api/agents/{id}/screenshot (format=text|ansi|html, rows/cols) backed by the tail of transcript.log through atn_pty::snapshot. Proper Content-Types, 400 on bad format, 404 on unknown id. New integration test spawns fake-claude, polls for banner, exercises every response path. Drive-by fix to router: send text+\r atomically so event_log is consistent with inbox-write (fixes three_agent flake — 5/5 clean runs).