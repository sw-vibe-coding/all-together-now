git-sync-agents saga close-out: atn-syncd integration test exercises the real binary (env!(CARGO_BIN_EXE_atn-syncd)) against a tempdir bare-central + worktree + prs-dir fixture, asserts pushed ref + PrRecord round-trip + queued-marker rename + clean no-marker exit. New docs/git-sync-agents.md (architecture sketch, marker format, CLI/exit-codes/lifecycle, REST, atn-cli prs, PrRecord shape, limitations). New demos/git-sync/setup.sh: mktemp workspace → bare central + clone-into-alice + clone-into-bob → boot atn-server with ATN_PORT=0 + parse 'atn-server ready on' → spawn one atn-syncd per worktree → drop markers on both → poll prs-dir up to 12 s → atn-cli prs list/merge for each id → git -C central log confirms both Merge commits → trap-EXIT cleanup. Live-verified end-to-end: both PRs merged into central main, all daemons cleaned up on exit. docs/demos-scripts.md: Demo 13 with full step walkthrough + reject/conflict/dry-run variations + index row + 'multi-agent PR + merge' short-slot pick + git-sync-agents.md see-also. docs/status.md: Current State → Git-Sync-Agents Saga Complete, atn-agent → Prior Milestone, atn-syncd in What-Exists, 10 crates in Architecture, G1..G5 phase rows. cargo test --workspace + clippy --all-targets -D warnings + cargo doc --workspace --no-deps all clean.