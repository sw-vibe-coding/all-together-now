New atn_agent::llm module with typed Chat shapes + chat() helper (60s timeout, human-readable errors). Main loop threads PollCtx through handle_inbox_file; !dry_run builds ChatRequest with system+user prompts, calls llm::chat, prints content. tool_calls logged (dispatch lands step 3). LLM errors don't wedge the poll loop. 7 new unit tests (16 total). Live-verified dry-run + dead-port error paths.