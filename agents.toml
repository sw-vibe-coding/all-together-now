# ATN boots with zero agents by default. Use the "New Agent" button in the
# web UI to add local or remote agents at runtime. See agents.example.toml
# for the static-config shape if you prefer declaring agents up front.

[project]
name = "all-together-now"
log_dir = ".atn/logs"
