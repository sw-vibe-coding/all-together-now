# Changes

- side task: `factory-line/` Factorio-style 6-agent coordination demo + `docs/ui-walkthrough.md` end-to-end UI test plan + `agent_prompt` first-class spec field. New `crates/atn-core/src/spawn_spec.rs` field `agent_prompt: Option<String>` (`#[serde(default, skip_serializing_if = "Option::is_none")]`); compose_command appends ` '<prompt>'` (single-quoted, with the same FORBIDDEN_CHARS validation as the rest — single quotes inside the value rejected at validation time so the wrapping stays injection-safe). Static `validate()` loop adds the field; `static_label` adds the case. Existing struct-literal call-sites in `spawn_spec.rs::tests::{minimal_local, worker_hlasm}` add `agent_prompt: None`. Net effect: a fixture with `"agent_prompt": "read AGENTS.md and follow it"` gets `cd <dir> && claude 'read AGENTS.md and follow it'` so multi-word prompts survive the shell. Dashboard side: new `@keyframes atn-attn-pulse` (amber/yellow, distinguishable from the existing amber stall pulse) on `.dashboard .agent-panel.attn-needed`, `.dashboard .agent-compact.attn-needed`, `.spark-cell.attn-needed`. The existing `pollStates` wrapper (which already played a sine-sweep notification on transition into `awaiting_human_input | error | blocked`) gains a class-toggle that adds/removes `.attn-needed` on the panel + compact + sparkline cell when `wantsAttn && !focused` (selectedId comparison). `windowSelect(id)` clears the class on the focused tile + its compact + its sparkline so click-to-focus dismisses the pulse. New `~/github/softwarewrighter/factory-line/` (out-of-tree) project: 6 agent dirs (`coordinator`, `gatherer-iron`, `gatherer-coal`, `smelter`, `stamper`, `assembler`), shared `inventory/` + `output/`, one short `AGENTS.md` per agent describing the per-stage recipe (1 iron + 1 coal → 1 ingot; 1 ingot → 1 widget; 1 widget → 1 gadget OR 1 gizmo), plus a parameterized `atn-send` (single script copied to each subdir; derives `source_agent` from `$(basename $PWD)` so one file works for all 6). New `demos/factory-line/{setup.sh, kickoff.sh, drive-bash.sh, fixtures/*.json}` (10 files): `setup.sh` POSTs all 6 fixtures (with `ATN_DEMO_AGENT=bash` override that strips `agent_prompt` not implemented yet — see open follow-up); `kickoff.sh` POSTs a single goal prompt to coordinator's `/input` endpoint; `drive-bash.sh` is the bash-mode wiring smoke test that drives the full DAG via direct `/api/agents/<id>/input` POSTs (no AI), waits for each stage's Done event to land in `/api/events` before triggering the next, and asserts final `output/widget.txt` contents — exit 0 means router round-trip works in both directions. New `docs/ui-walkthrough.md` lays out the empty → 1-coord → ... → 6-agent → full-goal phase table with API-shape + DOM-selector reference for each tab (Agents/Graph/Saga/Wiki/Events), records phase-by-phase results, and documents three findings surfaced during the walkthrough: (a) the graph view always draws a `coordinator → worker` edge per non-coord agent unconditionally (`static/index.html:4431-4438`), so edge count is `(N-1) + sum(blocked_on)` and `feature_request` events do NOT change the edge count; (b) the wiki backing dir survives `pkill -9 atn-server` so empty-wiki baselines must explicitly `rm -rf .atn/wiki/`; (c) real CLIs (claude/codex/opencode) start with interactive permission/trust dialogs that consume router-typed input as menu answers — claude additionally hits a `Checking for updates` loop that pegs the PTY before any input is read; bash-mode walkthrough verified by full E2E run (5 events all `delivered=true`, `output/widget.txt` correct). Side fix in three sibling out-of-tree repos (uncommitted; out of this repo's tree): `~/github/softwarewrighter/atn-{blog,explainer,short}/atn-send` source_agent identity patches (per the prior blog-vlog commit) carry forward unchanged. Side touch in tree: `demos/blog-vlog/fixtures/{blog,explainer,short}.json` agent-CLI assignment switched from `claude` everywhere to per-user-preference (`blog: codex`, `explainer: opencode`, `short: codex`) per memory `feedback_multi_agent_coexist.md`. New `demos/blog-vlog/kickoff.sh` (untracked sibling of the existing `setup.sh`) — POSTs the single-paste brief to coordinator's PTY input. cargo build clean.
- side task: `demos/blog-vlog/` scaffolding so a single command boots a real four-agent coordination demo backed by the kitted `~/github/softwarewrighter/atn-{coordinator,blog,explainer,short}` repos. New `demos/blog-vlog/setup.sh` (~140 lines, `chmod +x`'d): pre-flight check that the three worker repos exist + auto-create the coordinator workspace dir, reuse-or-boot atn-server (`ATN_DEMO_SKIP_BOOT=1` short-circuits, `ATN_DEMO_URL` overrides the base URL, default `http://localhost:7500`), poll `/api/agents` until ready, then POST four fixtures via curl. New `demos/blog-vlog/fixtures/{coordinator,blog,explainer,short}.json` — all `transport: local`, `agent: claude`, absolute `working_dir` paths (so the existing `~`-doesn't-expand-in-Rust-`Path` failure mode that left coordinator windows at cwd=~ with no agent running can't recur). `ATN_DEMO_AGENT=<cli>` swaps the agent CLI for all four via a python3 in-place rewrite of `spec['agent']` before POST. No fake-shim mode here, unlike `demos/three-agent/setup.sh` — `tools/fake-claude` is a stdin echo loop and can't run `atn-wiki`/`./atn-send`, which the AGENTS.md-driven coordination protocol requires. Side fix in three sibling repos (out-of-tree, not committed here): `~/github/softwarewrighter/atn-{blog,explainer,short}/atn-send` all hardcoded `source_agent: "coordinator"` (and `source_repo: "coordinator"`), so a worker's `./atn-send coordinator "Done: ..."` would have arrived at the message router with both source and target set to `coordinator` — patched each script to its own identity (`blog`/`explainer`/`short`). New `~/github/softwarewrighter/atn-coordinator/{AGENTS.md, atn-send}` workspace (also out-of-tree): `AGENTS.md` is a coordinator brief — read or seed `Coordination__Goals`, initialize `Coordination__Agents`, brief blog/explainer/short with one `./atn-send` each, then watch `Coordination__Log`/`Blockers`/`Requests` and route work — and `atn-send` is the existing helper (its hardcoded `source_agent: coordinator` is correct here so no edit needed).
- dashboard-prs saga step 1 (`/api/prs/stream` SSE endpoint + filesystem watcher): drives the upcoming dashboard PR panel without polling. New `crates/atn-server/src/prs_stream.rs` (~440 lines including tests): `PrsEvent` snake-case-tagged union (`Snapshot { records } / Created { record } / Updated { record } / Removed { id }`) so `EventSource` clients can `match` on the `event` field directly; `PrsBroadcast` thin wrapper over `tokio::sync::broadcast::Sender` (capacity 256 — generous so a slow tab doesn't lose intermediate updates) with `send` (best-effort, ignores no-subscribers error so mutating routes still succeed) + `subscribe`; `spawn_watcher(prs_dir, broadcast)` runs `notify::recommended_watcher` (workspace already pinned to `macos_kqueue` backend, default-features-off) on a dedicated OS thread, pushes raw events through a `std::sync::mpsc` channel, and a coalescing loop drains everything that arrives within `COALESCE_WINDOW = 50 ms` before translating — so a `tempfile + rename` write doesn't fan out as two `Updated`s. `translate_and_broadcast(prs_dir, events, broadcast, seen)` walks the burst's path set, partitions into `touched` vs `removed`, reads the JSON, parses `PrRecord` (skips on parse error with `tracing::warn!`), and emits `Created` for first-time ids vs `Updated` for ones the per-watcher `HashSet<id>` already knows. Removed paths emit `Removed { id }` only if the id was previously known (no spurious removes for files that never existed). `pr_stream(State<AppState>)` async handler: subscribes to the broadcast, spawns a `tokio::task` that emits a `Snapshot { records }` (from a `spawn_blocking` `read_records` call) on first connect then forwards every broadcast event verbatim, lifts a `tokio::sync::mpsc::Receiver<Event>` into a stream, and wraps in `Sse::new(...).keep_alive(...interval(15s))`. `tokio::sync::broadcast::error::RecvError::Lagged(n)` logs at warn and keeps the stream open; `Closed` ends it. atn-server boot: canonicalize `prs_dir` after `create_dir_all` (kqueue doesn't always resolve relative paths) then call `spawn_watcher` once before route registration; logs `prs watcher: watching <abs path>` at info. New `/api/prs/stream` route. Mutating routes (`merge_pr` + `reject_pr`) push `PrsEvent::Updated { record: rec.clone() }` onto the broadcast after the JSON write succeeds, so dashboards see the new state even on platforms that collapse `tempfile + rename` into one `Modify` event (the client de-dupes by `id + status`). `PrsState::new` gains a third arg (the broadcast). 11 new unit tests in `prs_stream::tests`: serde round-trip + event-tag assertions for all four variants, broadcast send-with-no-subscribers is silent, subscriber-receives-send happy path, `translate_and_broadcast` emits Created-then-Updated for the same id (driven via synthesized `notify::Event`s with `EventKind::Create` then `Modify`), `Remove` for known-id, non-JSON files are ignored, create+remove burst on an unknown id stays silent. New integration test `prs_stream_pushes_a_create` in `prs_endpoints.rs`: opens an `EventSource`-style stream via `curl -sN`, drops a fresh PR JSON 700 ms in, asserts both the snapshot and the `created` event with the new id arrive within 5 s. Live-verified end-to-end against a running atn-server: snapshot → write file → `created` event → modify file → `updated` event with new status/merge_commit → rm file → `removed` event with id. Side fix: 3 atn-cli `base_url` unit tests now share a `OnceLock<Mutex<()>>` `env_lock()` so concurrent `unsafe { env::set_var }` writes don't race during parallel test runs (pre-existing flake; surfaced when adding more workspace tests). cargo test workspace + clippy `--all-targets -D warnings` + cargo doc `--workspace --no-deps` all clean. Workspace test counts: atn-server 25 unit + 2 integration (1 new) — total 13 prs-stream-touched test cases.
- git-sync-agents saga step 5 (integration test + docs + Demo 13 + status): closes the git-sync-agents saga. New `crates/atn-syncd/tests/integration.rs` spawns the real `atn-syncd` binary (`env!("CARGO_BIN_EXE_atn-syncd")`) against a tempdir layout (`central.git` bare + `alice` worktree on a `feature` branch + empty `prs-dir`); two cases: (a) marker-present run pushes the branch to the bare remote, writes `<prs-dir>/alice-feature-<short>.json` (round-trips back into a `PrRecord` with `agent_id=alice`, `branch=feature`, `target=main`, `summary="demo PR from alice"`, `status=Open`, `commit==git rev-parse feature`, `id==alice-feature-<short>`) and renames the marker to `.atn-ready-to-pr.queued.<short>`; (b) no-marker run exits 0 without touching the prs-dir. New `docs/git-sync-agents.md` (≈190 lines): architecture sketch (one-host subset of the uber-use-case), marker file format with key/default table, `atn-syncd` CLI reference + exit codes + lifecycle, REST surface matrix (4 routes), `atn-cli prs` subcommands summary, on-disk `PrRecord` JSON shape, and known limitations (no GitHub PRs, no diff in dashboard, manual mirror-back, single-host, `--no-ff`-only). New `demos/git-sync/setup.sh` (≈170 lines, `chmod +x`'d): `mktemp -d` workspace → bare central + clone-into-alice + clone-into-bob → `cargo build` server/syncd/cli → boot `atn-server` with `ATN_PORT=0` and parse the resolved port from `atn-server ready on …` → spawn one `atn-syncd` per worktree → `printf` markers on both → poll `prs-dir` up to 12 s for both JSON files → `atn-cli prs list --status open` → `atn-cli prs merge` for each id → `git -C central log --oneline main` to confirm both `Merge refs/heads/pr/...` commits landed → `atn-cli prs list --status merged` → cleanup all child PIDs + tempdir on `trap EXIT`. Live-verified: both demo runs end-to-end with no manual intervention; central log shows both merge commits. `docs/demos-scripts.md` gains Demo 13 (with full ten-step walkthrough + variations: reject path, conflict path, dry-run), an index row, a "Multi-agent PR + merge story" short-slot pick, and `docs/git-sync-agents.md` in the See-also list. `docs/status.md` flips Current State to **Git-Sync-Agents Saga Complete** (with bullet-summary of atn-syncd + REST surface + atn-cli prs + the demo + 30 unit + 4 integration tests), promotes atn-agent to a Prior Milestone, lists `atn-syncd` in the What-Exists table, bumps the Architecture list to **10 crates** with `atn-syncd` line, and adds G1..G5 phase rows. Workspace tests count: 39 + 30 + 14 + 13 + 49 + 22 + 11 + 8 + 4 + 2 (atn-syncd integration) + 2 (atn-cli integration) + 1 (prs_endpoints integration) + others. `cargo test --workspace` / `clippy --workspace --all-targets -D warnings` / `cargo doc --workspace --no-deps` all clean.
- git-sync-agents saga step 4 (atn-cli `prs` subcommand): wires the typed CLI against the new `/api/prs` REST surface so humans + scripts can drive the PR flow without curl. New `Prs` arm on the top-level `Command` enum dispatches `PrsCommand::{List, Show, Merge, Reject}`. `prs list [--status open|merged|rejected] [--format json|table]` GETs `/api/prs` (URL-encodes the filter via `PrStatusArg::as_str`) and renders a 5-col table — `ID  AGENT  BRANCH → TARGET  STATUS  SUMMARY` — with the summary truncated to 80 chars (`…` ellipsis on overflow, char-count not byte-count so it stays UTF-8 safe). Empty result prints `(no prs)`. `prs show <id> [--format json|table]` GETs `/api/prs/{id}`, table mode prints one `key:           value` line per field with merge_commit / merged_at / rejected_at / last_error only included when populated; 404 → exit 2 with `pr '<id>' not found` on stderr. `prs merge <id>` POSTs `/api/prs/{id}/merge`, prints the updated record on 200, and on 409 invokes `report_pr_conflict(action, id, body)` which parses the server's `{error, stderr}` (merge conflict path) or `{error, status}` ("not open" path) and surfaces the most useful field to stderr — exits 2 to match the wiki ETag-mismatch convention. `prs reject <id>` POSTs the parallel reject endpoint (no git side-effects, same conflict handling). New `atn-core = { workspace = true }` dep on atn-cli so the typed `PrRecord` / `PrStatus` shapes are reused (no duplicated DTOs). Both action handlers share `prs_action(base, verbose, id, action)` so `merge` and `reject` go through the same body/error pipeline. 10 new unit tests (30 total in atn-cli, up from 20): table layout asserts header set + branch→target cell + status + summary, summary truncation produces 79 chars + `…`, empty-vec table is just header+rule, `format_pr_show` for an Open PR omits all four lifecycle fields, for a Merged PR includes `merge_commit` / `merged_at` / `last_error`, `pr_status_str` round-trip, `PrStatusArg::as_str` URL forms, `truncate` short-string + max-cap, conflict body with `stderr` parsed cleanly, conflict body with only `status` falls back without panic, garbage 409 body still exits 2. New `atn_cli_prs_round_trip` integration test in `crates/atn-cli/tests/integration.rs` (separate `PrsServerGuard` from the existing `ServerGuard` — fewer entanglements): tempdir builds a non-bare central + clone, branches off + commits + pushes `pr/alice-feature`, drops a hand-crafted PrRecord JSON into the `prs-dir`, boots `atn-server --prs-dir … --central-repo …` with `ATN_PORT=0`, then drives the CLI against the resolved URL: `prs list` (table contains id + `feature → main` + `open`); `prs list --status merged` (empty table marker); `prs show <id>` (`agent: alice` / `branch: feature` / `status: open`); `prs show <missing>` (exit 2 + "not found"); `prs merge <id>` (exit 0; stdout body has `"status": "merged"` + `"merge_commit"`; central main `git log --oneline` shows the new `Merge refs/heads/pr/alice-feature` commit); `prs list --status merged --format json` (id surfaces); second-merge attempt (exit 2 + stderr surfaces "not open"). cargo test workspace + clippy `--all-targets -D warnings` all green.
- git-sync-agents saga step 3 (`/api/prs` REST surface): exposes the PR registry through atn-server so the dashboard + atn-cli have a single read/write API. New `crates/atn-server/src/prs.rs` (≈360 lines): `PrsState { prs_dir, central_repo, lock: Arc<Mutex<()>> }`, `read_records(prs_dir) -> Vec<PrRecord>` (sorted lexically, skips parse failures with `tracing::warn!`), `read_record(prs_dir, id) -> Option<PrRecord>` with id sandboxing (rejects empty / `/` / `\\` / `..` / `\0`), `write_record(prs_dir, &record)` atomic via tempfile + rename, `git_merge(central_repo, agent_id, branch)` running `git -C <central> merge --no-ff -m "Merge refs/heads/pr/<agent>-<branch>" <ref>` then `git rev-parse HEAD` for the merge SHA — on non-zero exit, captures combined stdout+stderr (conflict markers go to stdout, "Automatic merge failed" to stderr) and runs `git merge --abort` to leave the central worktree clean. Four route handlers wrap `tokio::task::spawn_blocking` for the file/git work and a single `Mutex<()>` guard around mutations so concurrent merge/reject hits can't clobber each other: `GET /api/prs` (with `?status=open|merged|rejected` query filter), `GET /api/prs/{id}` (404 on miss), `POST /api/prs/{id}/merge` (200 + updated record on success; 404 on missing id; 409 + `{error, status}` if not Open; 409 + `{error, stderr}` on conflict — flips `status: merged` + writes `merge_commit` (HEAD SHA) + `merged_at` (RFC3339)), `POST /api/prs/{id}/reject` (200 + record; 409 if not Open; flips `status: rejected` + writes `rejected_at`; no git side-effects). atn-server CLI grew two flags (`--prs-dir <PATH>` default `<base-dir>/.atn/prs`, `--central-repo <PATH>` default = parent-of-parent of the prs-dir, i.e. `base_dir`) parsed via a tiny `parse_server_args()` (positional config path + `--flag value` and `--flag=value` forms; unknown flags warn-log but don't crash). `SharedState` carries `prs: PrsState`. atn-server creates the prs-dir if missing on startup. 8 new unit tests in `prs::tests` (lexical sort + garbage-skip, round-trip + 404, path-traversal sandbox, atomic write leaves no `.tmp`, status filter, empty / missing dir, real `git_merge` conflict on a tempdir fixture asserting stderr surfaces `conflict|Automatic merge failed` and the central worktree is clean after `--abort`). New integration test `crates/atn-server/tests/prs_endpoints.rs`: tempdir fixture builds a non-bare central + clone, pushes two PR refs (`pr/alice-feature` + `pr/alice-feature-z`), drops two `PrRecord` JSON files into prs-dir, boots `atn-server --prs-dir … --central-repo …` with `ATN_PORT=0`, parses the resolved port from `atn-server ready on …`, then curls every route — list (unfiltered + `?status=open|merged|rejected`), get (200 + 404), merge (200 + central main contains `Merge refs/heads/pr/alice-feature` + `feature.txt` lands in worktree; second merge → 409), reject on the other PR (200 + `?status=rejected` finds it), and `merge` on a missing id → 404. Workspace test count up to 14 atn-server tests (8 unit + 1 prs integration + 5 prior); cargo test workspace + clippy `--all-targets -D warnings` all green.
- git-sync-agents saga step 2 (detect → push → record): replaces step 1's stub handler with the real out-of-band PR flow. New `parse_marker(&str) -> MarkerSpec` reads `key=value` body lines (whitespace-trimmed, `#`-comments and blank lines tolerated, unknown keys ignored, empty values skipped); recognized keys are `branch`, `target`, `summary`. Defaults: `branch = git rev-parse --abbrev-ref HEAD` in the worktree (HEAD/empty refused as a safety check), `target = "main"`, `summary = "<branch> ready for review"`. New `handle_marker(marker, &HandlerCtx)` orchestrates: read+parse marker → resolve branch → `git push <remote> <branch>:refs/heads/pr/<agent-id>-<branch>` → `git rev-parse <branch>` → build `id = <agent-id>-<branch>-<short-sha>` (first 7 chars) → write pretty-JSON `PrRecord { status: Open, created_at: chrono::Utc::now().to_rfc3339(), source_repo: <canonical repo path> }` to `<prs-dir>/<id>.json` → rename `<repo>/<marker>` to `<repo>/<marker>.queued.<short-sha>`. All-or-nothing semantics: any failure (push, rev-parse, write) leaves the marker in place so the next poll retries; only successful end-to-end flow renames. Idempotency guard: if `<marker>.queued.<short-sha>` already exists, log a warning and leave the original marker alone (caller still gets `HandleOutcome` populated). New `HandleOutcome { branch, pr_id, short_sha, record_path, renamed_marker }` so the success log prints the full picture (`pushed <branch> → pr/<id> (commit <sha>); record <path>; queued <queued-path>`). New `HandlerCtx { repo, agent_id, remote, prs_dir, dry_run, verbose }` — `poll_once` takes one and dispatches to `handle_marker` (skipped under `--dry-run`, which keeps the step-1 stub log). `git -C <repo> ...` shell-outs via `std::process::Command` (no git2 dep); failures capture stderr into the returned error string with the join'd args + exit code. The PrRecord shape is final-form for step 3's REST surface (atn-server `/api/prs`). 7 new unit tests (13 total in atn-syncd): marker parser empty/known-keys/blank-comment-tolerant/empty-value-skip, short_sha truncation; tempdir-fixture handler tests (`fixture_repo()` builds a bare `central.git` + worktree on `main` with one commit and a `central` remote): (a) success path asserts `for-each-ref` shows `refs/heads/pr/alice-main`, JSON parses back into `PrRecord` with the right branch/target/summary/status/commit prefix, original marker is gone and `.queued.<sha>` exists; (b) marker overrides (branch=feature-z, target=develop) push the right ref + record the right metadata; (c) push failure (remote pointing to nonexistent path) returns `Err`, leaves marker in place, and writes no record. Live smoke against a real bare remote pushes `pr/alice-main`, writes `prs/alice-main-7d80570.json`, and renames the marker to `.queued.7d80570` in one tick. cargo test workspace + clippy `-D warnings` clean (workspace test count: 39 + 13 + others, all green).
- git-sync-agents saga step 1 (atn-syncd scaffold + marker detection): opens a fifth saga from `docs/uber-use-case.md` — out-of-band git PR flow where each agent's `.atn-ready-to-pr` marker triggers a daemon-side branch push + JSON record so the dashboard / atn-cli can surface review-ready work. New `crates/atn-syncd/` workspace member with `[[bin]] name = "atn-syncd"`. CLI (clap derive): `--repo` (required), `--agent-id` (required, rejects `/` `\\` `..` and empty), `--remote` (default `central`), `--marker` (default `.atn-ready-to-pr`), `--prs-dir` (default `.atn/prs`), `--poll-secs` (3), `--dry-run`, `--exit-on-empty` (clean exit after one marker-free pass — for tests), `--verbose`. `resolve_marker_path(repo, marker)` rejects empty / absolute / parent-dir traversal. Main loop prints `atn-syncd: <id> watching <repo> (marker=…, prs-dir=…)`, ensures `--prs-dir` exists, then polls every `--poll-secs`; on marker presence the step-1 stub logs `would handle marker at <path> (dry-run)` (step 2 wires push + record). New `atn-core::pr` module: `PrRecord { id, agent_id, source_repo, branch, target, commit, summary, status, created_at, merge_commit?, merged_at?, rejected_at?, last_error? }` + `PrStatus { Open, Merged, Rejected }` (snake_case), `filename()` helper, optional fields `skip_serializing_if Option::is_none`. 12 new unit tests (6 in atn-core::pr — serde round-trip, snake_case status, merged metadata, rejected metadata, filename, optional-omission; 6 in atn-syncd — resolve happy path, empty/absolute/parent-traversal rejects, poll_once toggle, atn-core::pr wiring smoke). Acceptance smoke: `cargo run -p atn-syncd -- --repo /tmp/atn-syncd-smoke --agent-id alice --dry-run --exit-on-empty --verbose` prints the banner, polls once, and exits clean; with the marker present it logs `would handle marker at … (dry-run)` per tick. cargo test workspace / clippy `-D warnings` clean.
- atn-agent saga step 5 (integration test + `docs/atn-agent.md` + Demo 12): closes the atn-agent saga. New `crates/atn-agent/tests/integration.rs` spawns an in-process `TcpListener`-backed HTTP stub that parses HTTP/1.1 requests + hands out canned `ChatResponse` JSON in sequence — no external Ollama needed. Two test cases: (1) model calls `file_write` → `outbox_send` → final text → loop exits; asserts the workspace file + `.atn/outboxes/demo/agent-demo-<millis>.json` PushEvent + `ev-42.json.done` rename all landed, banner + tool logs + final content on stdout. (2) model calls `shell_exec` without `--allow-shell`; tool returns `{disabled: true}`, model recovers, inbox still renames. Runs clean in ~1.2 s. New `docs/atn-agent.md` covers CLI flag table with defaults, exit-code table, per-tool reference with examples, security notes (path sandboxing / shell gating / workspace-relative scope / transport timeouts), and testing pointers. New `demos/atn-agent/setup.sh` with `--stub` (self-contained via `cargo test`) and real-Ollama (seeds `.atn-demo-atn-agent` with an inbox message, runs the agent with `--exit-on-empty --allow-shell`, prints inbox/outbox/workspace after) modes. `docs/demos-scripts.md` gains Demo 12 with setup/steps/cleanup/variations for both modes, an index row, a "Rust-native AI-agent story" short-slot entry, and `atn-agent.md` in the See-also list. `docs/status.md` flips Current State to **atn-agent Saga Complete**, promotes Dashboard-Polish to Prior Milestone, adds A1..A5 rows, and lists `atn-agent` in both the What-Exists table (9 crates) and the architecture list. 2 new end-to-end tests (41 test cases total across atn-agent); cargo test workspace / clippy `-D warnings` / doc all clean.
- atn-agent saga step 4 (shell_exec + outbox_send + inbox_ack tools): rounds out the tool kit. `shell_exec(command)` is gated on `--allow-shell` — when disabled it returns `{disabled: true, message: …}` (not an error, so the model stops calling it). When enabled it runs `/bin/sh -c <command>` with `current_dir = workspace`, polls `try_wait` every 100 ms, kills the child after `SHELL_EXEC_TIMEOUT = 30 s` (returns whatever was captured + `timed_out: true, notice: killed after 30s`), and packs `{command, exit_code, bytes, output, truncated?, timed_out?}` with combined stdout+stderr trimmed to `SHELL_OUTPUT_MAX = 4 KiB` + `…truncated (N bytes total)` marker when over. `outbox_send(target?, kind, summary, priority?)` validates `kind` against the full `PushKind` enum with hyphen aliases (`bug-fix-request` → `bug_fix_request`), validates `priority` among `normal` / `high` / `blocking` (defaults to normal), synthesizes `agent-<id>-<millis>` ids + RFC3339 timestamps, builds a `PushEvent` with `source_agent = ctx.agent_id`, `source_repo = "."`, and writes pretty JSON to `<atn_dir>/outboxes/<agent_id>/<event_id>.json` so the message router's existing 2 s poll picks it up. Broadcast (no `target`) works. `inbox_ack(message_id)` walks the path safeguards (rejects empty / `..` / path separators on the id), then renames `<atn_dir>/inboxes/<agent_id>/<message_id>.json` → `.json.done`; missing file returns `"inbox message <id> not found at <path>"` cleanly. `ToolCtx`'s `agent_id` + `atn_dir` + `allow_shell` lose their `#[allow(dead_code)]` — all three are live now. `tool_schemas()` grows descriptions for the new tools (models read these to decide when to call). `parse_push_kind` + `parse_priority` helpers mirror atn-cli's validation so error messages list valid values in the same shape. 11 new unit tests (39 total): schema registry lists all 5 names, shell_exec gated off by default, allowed run captures stdout + exit code 0 and 7, missing-command error, outbox_send writes a parseable PushEvent with correct target/kind/priority/id-prefix/summary, hyphen-kind alias works + broadcast (no target), bad kind / bad priority return typed errors with valid-values hints, inbox_ack rename happy path, missing-file clean error, forbidden-id rejection (empty, `..`, `/`, `\\`). cargo test + clippy + doc clean.
- atn-agent saga step 3 (file_read + file_write + tool-call dispatch loop): new `atn_agent::tools` module with `ToolCtx { workspace, agent_id, atn_dir, allow_shell }`, `ToolResult { ok, content }` (ok_value/err ctors — errors still surface to the model as a `tool` message so it can course-correct), `dispatch(&ToolCall, &ToolCtx)`, and `tool_schemas()` returning the static Ollama function-tool descriptors. `sandbox_path(workspace, user_path)` walks path components and rejects empty / absolute / `..`-traversal / root / windows-prefix ids before `workspace.join(p)` — table-driven tests cover the usual escape attempts. `file_read` caps at `FILE_READ_MAX = 256 KiB` and returns a `{path, bytes, content, truncated, notice}` payload so the model knows when content was clipped. `file_write` caps at `FILE_WRITE_MAX = 1 MiB`, creates parent dirs, returns `{path, bytes, message}`. `normalize_arguments` tolerates the Ollama-community quirk where some local models serialize `arguments` as a JSON string literal instead of an object. `main.rs` gains `PollCtx.tool_ctx + max_tool_iterations` and a new `run_tool_loop` that replaces the step-2 single-shot call: builds system+user messages, sends `ChatRequest` with `tools: Some(tool_schemas())`, iterates up to `--max-tool-iterations` — each turn it prints any text content, dispatches every `tool_call` (synthesizing an id when the model omitted one), appends the assistant's tool-call turn plus each `{role:"tool", content, tool_call_id}` reply to the conversation, and exits when the model returns no tool_calls or the iteration cap hits (stderr warning in that case). LLM transport errors still log + continue (step-2 semantics preserved). 12 new unit tests (28 total): sandbox accepts relative / rejects absolute + parent-dir + empty, file_read round-trip + truncation path (content length == FILE_READ_MAX), file_write round-trip + oversized rejection, dispatch unknown tool, stringified-argument normalization, and schema registry includes both names. cargo test + clippy + doc clean.
- atn-agent saga step 2 (Ollama /api/chat integration): new `atn_agent::llm` module with typed `ChatMessage { role, content, tool_calls?, tool_call_id? }` / `ToolSchema` (`{type: "function", function: {name, description, parameters: JSON-Schema}}`) / `ToolCall { id?, type=function, function: {name, arguments: Value} }` / `ChatRequest { model, messages, stream, tools? }` / `ChatResponse { message, done }` matching Ollama's non-streaming `/api/chat` shape (opencode mirrors it, so no provider enum needed yet). `chat(base_url, &req)` POSTs JSON with a 60 s timeout via `ureq::post(url).timeout(CHAT_TIMEOUT).send_json(...)` and returns `Result<ChatResponse, String>` — human-readable error messages bubble up transport failures + status codes + decode failures with the URL and body in context. Constructors: `ChatMessage::system`, `ChatMessage::user`, `ChatMessage::tool(tool_call_id, content)` (carries `tool_call_id` for step-3's tool response turn). `tool_calls` and `tool_call_id` use `skip_serializing_if = "Option::is_none"` so everyday messages stay lean; `default_tool_call_type` provides `"function"` when older Ollama responses omit `type`. Main loop now threads a `PollCtx { agent_id, base_url, model, dry_run, verbose }` through `poll_inbox_once` / `handle_inbox_file`; when `dry_run=false`, builds a `ChatRequest` with a `system_prompt_for(agent_id)` (canonical "you are ATN agent …") + `user_prompt_for(msg)` (`[from <src>] <summary>` + optional wiki_link + issue_id), calls `llm::chat`, and prints `message.content` to stdout. If the response includes tool_calls, the loop logs `atn-agent: model requested tool <name> (will dispatch in step 3)` per call — step 3 adds real dispatch. Transport / decode errors log to stderr as `atn-agent: llm-error: <msg>` and the main loop keeps polling (one bad LLM call shouldn't wedge the agent). 7 new unit tests (16 total): minimal ChatRequest serialization (with/without tools), ChatResponse content-only + tool_calls round-trips (including missing-id fallback), `ChatMessage::tool` serializes `tool_call_id`, transport-error message includes the URL, system prompt names the agent + lists tools, user prompt includes source + wiki_link + issue_id. Live smoke against a dead port: `--dry-run` prints `would POST /api/chat for ev-1` twice; real call emits `llm-error: transport error calling http://127.0.0.1:1/api/chat: Connection refused` and still renames both files to `.json.done` so a broken LLM doesn't stick a backlog in place. cargo test + clippy + doc clean.
- atn-agent saga step 1 (scaffold + inbox poll): new `crates/atn-agent/` workspace member with a clap-derive CLI + lifecycle loop that polls a per-agent inbox directory. Flags cover every eventual knob (required `--agent-id`; defaults on `--base-url http://localhost:11434`, `--model qwen3:8b`, `--atn-dir .atn`, `--workspace .`, `--inbox-poll-secs 2`, `--max-tool-iterations 8`; booleans `--allow-shell`, `--dry-run`, `--exit-on-empty`, `--verbose`) even though steps 2–4 fill in the ones that aren't wired yet. On startup the binary prints `atn-agent: <id> up (model=<m>, base_url=<b>)` so ATN's PTY state tracker flips the agent to `Running` immediately, then loops every `--inbox-poll-secs` reading `*.json` files out of `<atn-dir>/inboxes/<agent-id>/` (sorted lexically so timestamp-prefixed event ids come through in timeline order), deserializing each as `atn_core::inbox::InboxMessage`, printing `inbox: <id> — <summary>` to stdout, and renaming the file to `.json.done` to prevent reprocessing. Malformed JSON is logged to stderr and the file is left in place so an operator can inspect it. `--dry-run` adds a `would POST /api/chat for <id>` line instead of the (step-2) LLM call. `--exit-on-empty` returns cleanly after the first empty poll — this is the primary tear-down path for the integration test; production relies on SIGINT from ATN. `resolve_inbox_dir` defends against `..` / absolute / path-separator agent ids (defense-in-depth over the trusted atn-server path). 7 unit tests cover defaults, path rejection, empty-dir no-op, happy rename, malformed-JSON skip, and a const-drift check against `ATN_DIR_NAME`. Live smoke: seed an inbox file, run `atn-agent --agent-id demo --atn-dir <tmp> --dry-run --exit-on-empty --verbose`, confirm banner + `inbox: ev-42 — Implement the thing` + `would POST /api/chat` + second-pass empty + rename to `.json.done` + exit 0. cargo test + clippy + doc clean.
- dashboard-polish saga step 5 (docs + Demo 11 + status rows): closes the dashboard-polish saga. New `docs/events-view.md` covers the layout (escalation banner lane + two columns), the filter bar (text / kind-chips OR / delivered radio / reset / count / persistence in `atn-window-ui-v1`), detail-expand (Event ID, Timestamp abs+rel, Priority, Source repo, Issue ID, linkified Wiki link, Delivered, Decision + full JSON), wiki-link reuse when the panel is open, escalation → jump-to-event, and the REST surface the page reads from. `docs/windowed-ui.md` gains a **Wiki side panel** section covering the top-bar toggle, page picker, ETag-polled live updates with `wsp-flash` keyframe, visibilitychange pausing, and the events cross-link; the Persistence example is updated to show the `eventsFilter` + `wikiPanel` sub-keys. `docs/demos-scripts.md` gains **Demo 11 — Events view + wiki panel** (setup → 7 guided steps → cleanup → variations) mirroring the Playwright smoke from steps 1–4 (seed 2 agents + 5 events, filter, expand + Esc, escalation jump, open panel, live-edit via atn-cli to watch the flash, cross-link). Picking-one-for-a-short-slot gains a coordination / long-running-flow recommendation pointing at Demo 11; the See-also list now includes `docs/events-view.md`. `docs/status.md` Current State flips to **Dashboard-Polish Saga Complete**; atn-cli is promoted to Prior Milestone; D1..D5 rows added. cargo test / clippy / doc clean.
- dashboard-polish saga step 4 (wiki panel live updates + events-row cross-link): turned the read-only panel from step 3 into a live mirror of the selected wiki page, and lets an event's `wiki_link` reuse the panel instead of opening a new tab. **Server**: `wiki_get_page` now honors `If-None-Match` — when the header matches the current ETag, returns `304 Not Modified` + `ETag` header with an empty body. **Client**: new `wikiPanel.etag` state + `loadWikiPanelPage(title, mode)` where `mode='initial'` (user-driven — clears ETag + shows `Loading…`) or `mode='poll'` (background — sends `If-None-Match: <etag>`, no spinner, triggers a 350 ms green `.wsp-flash` keyframe animation on real re-render). `startWikiPanelPolling`/`stopWikiPanelPolling` wrap a 5 s `setInterval` whose body short-circuits when `!wikiPanel.open || document.hidden || !wikiPanel.title`. A `visibilitychange` listener fires an immediate `poll` load when the tab comes back so the panel catches up after a long background stretch. Deleted pages (404 on poll) now render `(page deleted)` muted instead of an error banner. New `openWikiPanelAt(title)` helper opens the panel + syncs the dropdown + refetches. **Events cross-link**: the `a.event-wiki-link` click branch in the delegated document handler checks `wikiPanel.open` — if true, it preventDefaults + calls `openWikiPanelAt(title)`; otherwise the anchor's `target="_blank"` default runs. Live verified: `curl -H 'If-None-Match: <etag>'` → `304` on match, `200` on mismatch; `loadWikiPanelPage('Coordination__Goals','poll')` after a REST PUT re-renders with `wsp-flash` class attached; panel closed → **0 fetches** during a 6.5 s wait (polling cleanly paused); event-row cross-link with panel open switches title from `Coordination__Requests` → `Coordination__Goals`, body swaps, `click.defaultPrevented=true`, `tabsOpened=0`. cargo test + clippy + doc clean.
- dashboard-polish saga step 3 (global wiki side-panel, read-only): new fixed-right drawer that mirrors a wiki page alongside the dashboard without reflowing the windowed-UI tile math. Top-bar `📖 Wiki panel` toggle (`.wiki-side-panel-toggle`, active-state amber/green) opens/closes the drawer, which slides via `transform: translateX(…)` transition — it sits at `position: fixed; right: 0; width: 340px; z-index: 20` with a shadow-left, so nothing in the dashboard's layout math moves. Panel markup: a `.wsp-head` with a `<select class="wsp-select">` page picker + a `✕` close button, and a `.wsp-body` scrollable region. `populateWikiPanelSelect` hits `GET /api/wiki`, sorts into options, and defaults to the saved title → `Coordination__Goals` → first page. `loadWikiPanelPage` calls `GET /api/wiki/{title}` and renders the server's `html` field (server already runs markdown → HTML with safe anchor handling). `.wsp-body` CSS restyles h1/h2/h3, lists, anchors (`#4ecca3`), and inline code blocks to fit the dashboard palette. New `wikiPanel = { open, title }` module state + `toggleWikiPanel(force?)` (auto-populates on open) + `setupWikiPanel` wiring. Persistence rides the existing `atn-window-ui-v1` blob under a new `wikiPanel` sub-key; loadWindowUiPrefs parses with type guards. Esc handler extended: event-expansion closes first, otherwise the wiki panel closes (guarded by `isTypingTarget` + `isXtermFocused`, so the Option-C window-management Esc only fires when nothing else is open). Live Playwright smoke: baseline hidden; click toggle → visible, active chrome, 5 `Coordination__*` pages in the dropdown, `Coordination__Goals` default, body rendered; pick `Coordination__Requests` → body swaps to the Requests page text; Esc → closed, toggle inactive; persisted blob = `{open: false, title: "Coordination__Requests"}`; reopen → the last selection is still bound. cargo test + clippy + doc clean (no Rust changes).
- dashboard-polish saga step 2 (inline event-row expansion + escalation jump link): every `.event-card` now carries a `data-event-id` and a hidden `.event-detail` section. Clicking a card toggles `.expanded`; the detail renders a two-column `<dl>` (Event ID / Timestamp w/ absolute local + `Xs/m/h/d ago` relative / Priority / Source repo / Issue ID / Wiki link as a clickable `/wiki/<title>` `a.event-wiki-link` opening in a new tab / Delivered yes-no / Decision) plus a `<pre>` with the full `EventLogEntry` pretty-printed JSON. Only one card expanded at a time (`expandedEventId` module state); clicking a second collapses the first. Pressing `Esc` collapses — guarded by `isTypingTarget` + `isXtermFocused` so it doesn't steal the Option-C window-management `Esc` when those are active. Escalation banners gain a `jump to event ▸` button (`.esc-jump`, `data-jump-event-id`) that scrolls the matching card into view via `scrollIntoView({behavior:'smooth', block:'center'})` and expands it. Expansion survives a refresh (e.g. after filter change) when the card is still visible; otherwise the state is cleared. Summary/from/target/timestamp now run through a shared `escHtml` helper so `<script>`-containing summaries can't land as live HTML. `formatAbsoluteTs` uses `toLocaleString`; `formatRelativeTs` buckets to s / m / h / d. Live Playwright smoke with 3 events (1 completion w/ `wiki_link=Coordination/Log`, 1 bug_fix w/ `issue_id=ATN-42`, 1 needs_info to a ghost target → escalation): baseline 3 cards + 1 banner + 1 jump button; click bug_fix → expands, detail shows Issue ID ATN-42 and JSON; click completion → bug collapses, completion expands and carries `a.event-wiki-link[data-wiki-title="Coordination/Log"]`; Esc collapses (0 expanded); escalation banner's jump button opens the `who owns the watchdog?` needs_info card. cargo test + clippy + doc clean (no Rust changes).
- dashboard-polish saga step 1 (Events filter chips + text search + delivered toggle): new `.events-filter-bar` above the two-column `.events-columns`. Wired client-side on top of the existing `GET /api/events` fetch — no new endpoints. Controls: a `#events-search` text input (case-insensitive substring match across `summary` + `source_agent` + `target_agent`), six kind chips (`feature_request`, `bug_fix_request`, `completion_notice`, `blocked_notice`, `needs_info`, `verification_request`) that OR within-category, a three-way `.ev-delivered` radio group (`all` / `delivered` / `undelivered`), a `✕ reset` button, and a right-aligned `.ev-count` label that reads `"N entries"` (no filter) or `"K / N entries"` (narrowed). New `eventsFilter = { text, kinds: Set, delivered }` module-level state + `eventsFilterMatches(entry)` predicate applied inside `eventsRefresh` so escalation banners stay unfiltered (the "needs attention" lane) while the two columns narrow. `renderEventsFilterBarState` syncs DOM → state on load; `setupEventsFilterBar` wires input/click listeners and persists state. Filter state rides the existing `atn-window-ui-v1` localStorage blob under a new `eventsFilter` sub-key: `saveWindowUiPrefs` serializes the Set as an array, `loadWindowUiPrefs` parses back with type guards on each field. Live Playwright smoke with 4 seeded events (1 completion, 1 bug_fix, 1 feature, 1 blocked broadcast): baseline `4 entries`; text `parser` → `1 / 4 entries` (only the bug_fix); kind `blocked_notice` → 1; `blocked_notice` + `feature_request` → 2 (OR); delivered=undelivered → 1 (only the broadcast); reset → `4 entries`; persistence round-trips the full state through the JSON blob. cargo test + clippy + doc clean (no Rust changes).
- atn-cli saga step 5 (integration tests + docs + Demo 10): closes the atn-cli saga. New `crates/atn-cli/tests/integration.rs` boots a real `atn-server` on an ephemeral port with `tools/` on PATH, captures the bound port from the `atn-server ready on <addr>` stdout marker, and drives every atn-cli subcommand group against it via `Command::new(env!("CARGO_BIN_EXE_atn-cli"))` — agents list (empty) → REST-seed a bash agent → `agents wait --state any-non-starting` → `agents input 'echo HELLO_FROM_CLI'` + `agents wait --state idle` → `agents screenshot` polls until output contains `HELLO_FROM_CLI` → `agents state --format json` sanity + unknown-id-exit-2 → `events send` + `events list` round-trip polled until the entry surfaces → bad-kind exit-1 with valid-values hint → `wiki get` seeded page → `--verbose wiki get` to capture ETag from stderr → `wiki put --stdin --if-match <etag>` success → stale-etag 409 exit-2 → scratch page create + delete + get-after-delete exit-2. Single test, runs in ~2 s. Added `tempfile` dev-dep to atn-cli for the temp-dir server workspace. New `docs/atn-cli.md` — intro + base-URL resolution + exit-code table + per-subcommand-group reference (`agents`, `events`, `wiki`) + 3 script recipes (seed + wait-for-all-idle, tail-since-watermark, ETag-retry loop with `$? == 2` branch) + a testing pointer. `docs/demos-scripts.md` gains **Demo 10 — atn-cli tour** with setup / 6-step flow / cleanup / variations that mirrors the integration test, plus a new entry in the at-a-glance index and a revised "Integrations / API-minded audience" recommendation in the Picking-one-for-a-short-slot section. `docs/status.md` current-state blurb reads "atn-cli Saga Complete"; windowed-UI promoted to "Prior Milestone"; new `C1..C5` rows in the phase table; `atn-cli` row added to What-Exists and the architecture crate list (now 8 crates). 18 atn-cli unit tests + 1 end-to-end integration test green; full workspace cargo test / clippy --all-targets / doc warning-free.
- atn-cli saga step 4 (wiki list/get/put/delete with ETag): new `wiki` top-level subcommand. `wiki list [--format table|json]` GETs `/api/wiki`, prints titles one per line (or JSON array); empty store prints `(no wiki pages)`. `wiki get <title>` GETs `/api/wiki/{title}` and streams the markdown body to stdout (doesn't URL-encode the title, since the server uses a wildcard path match that wants raw slashes like `Coordination/Goals`); `--verbose` writes the `ETag: …` header to stderr so scripts can capture it for subsequent writes. `wiki put <title> [--file <path> | --stdin] [--if-match <etag>]` PUTs `{content: …}` with optional `If-Match` header. `wiki delete <title> [--if-match <etag>]` DELETEs. 409 Conflict (the server's ETag-mismatch shape, not 412) is converted to `ETag mismatch for '<title>' — refetch and retry (current ETag: …)` on stderr and exit 2 so script loops can branch cleanly; the current ETag is extracted from the server's `WikiConflictResponse.current_etag` field. 404s also exit 2. `--file` and `--stdin` are mutually exclusive; supplying neither is a usage error (exit 1). Shared `read_body(file, stdin)` helper factors the source selection. 4 new unit tests (18 total): `read_body` mutual-exclusion + missing-source error messages + file path round-trip, and `report_etag_conflict` confirming the EXIT_NOT_FOUND contract against a realistic conflict payload. Live smoke against the seeded wiki: `wiki list` shows all 5 `Coordination__*` pages; `wiki get Coordination/Goals` prints the markdown; `--verbose wiki get` surfaces the ETag on stderr; `wiki put --stdin --if-match <etag>` round-trips a new body; a stale `--if-match` produces a clear 409 message with the fresh current ETag + exit 2; `wiki put Scratch/AtnCli` + `wiki delete Scratch/AtnCli --if-match <etag>` round-trips create/delete cleanly; subsequent `wiki get` on the deleted page exits 2. cargo test + clippy + doc clean.
- atn-cli saga step 3 (events list + send): new `events` top-level subcommand. `events list [--since N] [--format table|json]` GETs `/api/events[?since=N]`, renders a table with columns LOGGED_AT / KIND / FROM → TO (or `→ broadcast` when no target) / DECISION / DELIVERED / SUMMARY (truncated to 80 chars). `events send --from <a> [--to <a>] --kind <kind> --summary <t> [--priority normal|high|blocking] [--issue-id X] [--wiki-link P] [--source-repo .]` constructs a PushEvent with an auto `cli-<from>-<millis>` id + RFC3339 timestamp and POSTs flat JSON to `/api/events` (matches the server's `SubmitEventBody { #[serde(flatten)] event: PushEvent }` shape). Kind validation is client-side against the PushKind enum (`feature_request`, `bug_fix_request`, `completion_notice`, `blocked_notice`, `needs_info`, `verification_request`) with hyphenated aliases accepted; invalid values exit 1 with a one-line message listing valid options. Priority validation covers `normal`/`high`/`blocking`. New deps: `chrono` (for `Utc::now().to_rfc3339()` and `.timestamp_millis()`). 5 new unit tests (14 total atn-cli tests): kind parsing (canonical + hyphen), kind rejection carries the full list in the error, priority validation, `build_push_event` shape + auto id prefix + RFC3339 timestamp, events table rendering (routed + broadcast rows, delivered yes/no). Live smoke with a 2-agent topology: `events list` empty → `(no events)`; two `events send` POSTs (one canonical, one hyphen-alias kind with `--priority high`) both exit 0; `events list` a few seconds later shows both entries delivered, correct FROM → TO, decision `deliver:coord`. cargo test + clippy + doc clean.
- atn-cli saga step 2 (agents input/stop/restart/reconnect/delete/wait/screenshot): rounded out the `agents` subcommand with the write-path primitives demo scripts need. `input <id> <text>` POSTs `{text: text + "\r"}` to `/api/agents/{id}/input` — the atomic text-then-Enter shape from the UI's windowed-UI step-6 send fix, so a bash agent round-trips `pwd` / `echo HELLO_FROM_CLI` cleanly. `input <id> --stdin` slurps the full prompt from stdin (pipe multi-line without bash-escaping). `stop` / `restart` / `reconnect` are thin POST wrappers on `/api/agents/{id}/{action}`; `delete` DELETEs. `wait <id>` polls `/api/agents/{id}/state` with a doubling backoff capped at 4× `--poll-interval` (default 500 ms), respecting `--timeout` (default 30 s). `--state` accepts canonical snake_case (`idle`, `running`, `awaiting_human_input`, `completed_task`, ...) plus hyphenated aliases (`awaiting-input`, `completed-task`) and the umbrella `any-non-starting` that matches any state except `starting`. New `StateMatch` enum centralises the parsing + matching. `screenshot <id> [--format text|ansi|html] [--rows N] [--cols N]` GETs the snapshot endpoint and streams the body to stdout (respecting whatever Content-Type the server sends). 404s across all subcommands exit 2 with `agent '<id>' not found` on stderr; timeouts exit 3 with an explanatory line. 3 new unit tests cover the hyphen/snake form parsing, exact + `any-non-starting` matching against live state strings, and the `ScreenshotFormat::as_str` endpoint string mapping. Live smoke against a bash agent seeded via REST: `wait --state any-non-starting` → 0; `input 'echo HELLO_FROM_CLI'` + `wait --state idle` → 0, screenshot tail shows `HELLO_FROM_CLI`; `--stdin pwd` round-trips; `wait --state awaiting-input --timeout 2` correctly times out (exit 3); `delete` → 0; subsequent `state` → exit 2. 9/9 atn-cli unit tests green; clippy `-D warnings` + cargo doc clean; full workspace cargo test green.
- atn-cli saga step 1 (scaffold + agents list/state): new `atn-cli` workspace crate — a typed HTTP client for ATN that's meant to retire the `curl + jq` sprawl in the demo scripts. Deps kept lean (`clap` derive, `serde_json`, `ureq` sync HTTP — no async runtime, no reqwest bulk). Base-URL resolution: `--base-url <url>` flag → `ATN_URL` env → `http://localhost:7500` fallback. Exit-code convention baked into the top-level `--help`: 0 ok, 1 usage, 2 not-found, 3 http/transport, 4 server (5xx). First subcommands: `agents list [--format table|json]` (table columns: ID / NAME / ROLE / STATE / STALLED, right-padded, dashed rule under header; `(no agents)` for empty) and `agents state <id> [--format …]` (404 → exit 2 with `agent '<id>' not found` on stderr). JSON mode pretty-prints via `serde_json::to_string_pretty` for humans + diagnostics. 6 unit tests cover base-URL precedence (flag / env / default, all serializing env access), empty-table header-only output, right-padding + the `yes (Ns)` stalled rendering, `state_label` for simple + nested (`blocked.on = [...]`) agent states, and the JSON fallback when no `state` key is present. Live smoke against a running server: `agents list` with no agents prints `(no agents)`, with one bash agent prints a three-column aligned table; `agents state <id>` round-trips; `agents state nope` exits 2. `Cargo.toml` workspace gains `clap`/`ureq` hoisted deps and the new `crates/atn-cli` member. cargo test + clippy --all-targets + doc warning-free.
- Ops-polish saga step 6 (watchdog actions — Ctrl-C + blocked_notice + UI badge): closes the ops-polish saga. New `atn-server::watchdog_actor` background task (1 s poll) turns the per-agent stall signal from step 5 into real consequences. `WatchdogState` gains `stall_count_in_run: u32` that increments on every `0→1` stall edge and resets on `on_leaving_running`, so the actor can distinguish "Ctrl-C unstuck it" from "Ctrl-C was ignored and the agent went quiet again" even when the `^C` echo briefly clears the `stalled` flag. Policy: `stall_count == 1` → send Ctrl-C (`InputEvent::RawBytes { bytes: vec![0x03] }`); `stall_count ≥ 2` and still stalled → post a `blocked_notice` `PushEvent` to the agent's outbox (priority High, targeting the coordinator if one's live, else broadcast); when `running_for_secs >= max_running_secs`, escalate once with a separate `blocked_notice` about the running ceiling. Bookkeeping is per-agent and resets when `stall_count_in_run` returns to 0 (i.e. the agent left Running). UI: `.agent-panel.stalled`, `.agent-compact.stalled`, `.spark-cell.stalled` render an amber (`#f59e0b`) pulsing outline via `@keyframes atn-stall-pulse`; sparkline cell gains a 🚨 `.sc-stall` indicator and a tooltip like `stuck (wd) — stalled for 7s`. `pollStates` toggles the class on every agent every 2 s based on `agent.stalled` from `/api/agents`, and re-renders the sparkline row on any stall-flag flip to refresh the 🚨. Drive-by fix: the notifications hook at the bottom of the static HTML was REPLACING `pollStates` outright (overwriting the stall-class logic); now wraps via `origPollStates = pollStates` so both layers run. `docs/windowed-ui.md` gains a "Watchdog (stall detection)" section covering the chrome/sparkline badge, the 1s action cadence, per-agent `[watchdog]` toml + JSON config. `docs/status.md` Known-Issues list is now empty (the old "state tracking timing is fragile" bullet is superseded by step 5's watchdog + step 6's action loop). Live smoke against a Python agent that ignores SIGINT (`signal.signal(SIGINT, SIG_IGN); time.sleep(60)`) with `stall_secs=2`: +2s stall_count=1 → Ctrl-C (bash emits `^C` echo that clears `stalled`), +4s stall_count=2 → `blocked_notice` posted and routed to coordinator's `/api/events` log, +7s state→idle (IDLE_TIMEOUT). UI smoke via Playwright stub: `panel-stuck.className == 'agent-panel stalled'`, outline color `rgb(245,158,11)`, sparkline cell has `.stalled` + 🚨 + title "stuck (wd) — stalled for 7s". Closes the ops-polish saga. cargo test + clippy + doc clean.
- Ops-polish saga step 5 (per-agent output-stall watchdog): new `atn_core::watchdog::WatchdogConfig { stall_secs, max_running_secs }` (default 60s / None, optional `[watchdog]` section in `SpawnSpec`, carried through to `AgentConfig`). Runtime companion `atn_pty::watchdog::WatchdogState` tracks `last_output_at`, `running_since`, `stalled`, `stalled_since`; helpers `on_output`, `on_entering_running`, `on_leaving_running`, `check_stall` (one-shot — returns true only on the 0→1 edge), `stalled_for_secs`, `running_for_secs`. `spawn_state_tracker` now owns the watchdog handle: it updates `last_output_at` + `running_since` on every byte burst, clears stalled on leaving `Running` (PromptReady / QuestionDetected / IdleDetected / Disconnected), and polls `check_stall` at 1 s granularity via a `min(watchdog_tick, idle_deadline)` select. `PtySession::watchdog()` exposes the Arc<RwLock<WatchdogState>>. `AgentInfo` in the `/api/agents` + `/api/agents/{id}/state` responses gains `stalled: bool` and `stalled_for_secs: Option<u64>` (both skipped when idle / unset for a clean JSON). 13 new unit tests (4 in atn-core::watchdog + 9 in atn-pty::watchdog) cover TOML empty/partial/full sections, threshold crossings, output-resumption clearing, non-running-state immunity, running-since once-per-run, and the "exactly one stall fire per quiet period" invariant across bursts. Live smoke (bash agent with `watchdog.stall_secs = 3` + `sleep 60` input): +1s/+2s running/not-stalled, +3s stalled=true/stalled_for=0, +4s stalled_for=1, +5s idle/stalled=null (IDLE_TIMEOUT cleared it). cargo test + clippy + doc clean.
- Ops-polish saga step 4 (📸 snapshot button in window chrome): new per-window chrome icon `📸` between `▸ config` and `↻ reconnect` (same `.btn-icon` styling, `data-window-action="snapshot"`). Click fires `snapshotAgent(id)` which opens `/api/agents/<id>/screenshot?format=text&rows=40&cols=120` in a new tab (`noopener` for browser-security hygiene). Delegated chrome-click dispatcher routes the new action to the handler. `window.snapshotAgent` exposed for devtools. `docs/windowed-ui.md`'s chrome table gains a row for the snapshot affordance. Live Playwright smoke (3-agent demo): 📸 renders on every panel (`[coordinator, worker-hlasm, worker-rpg]`), `window.snapshotAgent` is `function`, click invokes `window.open('/api/agents/coordinator/screenshot?format=text&rows=40&cols=120', '_blank', 'noopener')`, direct fetch returns 200 text/plain with the live prompt content. cargo test + clippy + doc warning-free.
- Ops-polish saga step 3 (`GET /api/agents/{id}/screenshot`): new HTTP endpoint that returns a rendered terminal snapshot. Query params `format=text|ansi|html` (default `text`), `rows=N` (default 40, clamped 1..=500), `cols=N` (default 120). `text` and `ansi` return `text/plain; charset=utf-8`; `html` returns `text/html; charset=utf-8` with the self-contained `<pre>` from `atn_pty::snapshot::render_html`. Bad format returns 400 with a short `unknown format '<x>'; expected one of text|ansi|html`; unknown agent returns 404 with `agent '<id>' not found`. Source bytes come from the tail of `{log_dir}/{id}/transcript.log` — `read_transcript_tail` seeks to `len - min(rows*cols*8, 256 KiB)` and reads forward, so the snapshot reflects recent output without pulling the whole transcript into memory. New `SessionManager::log_dir()` accessor exposes the manager's logging root to the endpoint. New integration test `crates/atn-server/tests/screenshot_endpoint.rs` boots the real binary on an ephemeral port, spawns a `fake-claude` agent, polls until the banner lands in a `text` screenshot, then exercises every response path (text default, explicit `text`, `html`, raw `ansi`, `format=bogus` → 400, unknown id → 404). Drive-by router fix: `process_outbox_file`'s DeliverToAgent branch previously sent the prompt text then slept 150ms before sending `\r`, which produced a pre-existing flake in `three_agent_topology_end_to_end` where `/api/events` could report only 1 of 2 delivered events because the second `event_log.push` hadn't run yet when the test's both-inboxes poll fired. Folded the text + `\r` into one `HumanText` so the event log is consistent with the inbox-write milestone. 5-run soak of the three-agent test now passes deterministically. cargo test + clippy + doc warning-free.
- Ops-polish saga step 2 (vt100-driven terminal snapshot core): new `atn_pty::snapshot` module that parses any PTY-output byte stream through a `vt100::Parser` at a given rows × cols geometry and produces a `TerminalSnapshot { rows, cols, cells: Vec<CellRow> }` with one `Cell { ch, fg, bg, bold, italic, underline, inverse }` per grid position. Three rendering targets share one source of truth: `render_text()` strips style, `render_ansi()` emits SGR escapes with per-row resets so paste-into-terminal behaves, and `render_html()` emits a self-contained `<pre>` with inline-styled `<span>` runs (coalesces same-style cells, maps the standard 16-colour + 6×6×6 cube + 24-step greyscale palette to CSS `rgb` hexes). Geometry is clamped to ≥ 1×1 to dodge vt100's zero-size panic. `vt100 = "0.15"` hoisted into the workspace dependency table so `atn-pty` and `atn-replay` both pull from the same pin. 11 unit tests cover cursor-move + overwrite, clear-screen, ANSI colour / bold round-trips, HTML escaping, zero-geometry clamp, empty-input grid, palette spot-checks, and the `same_style` cell coalescer. `atn-replay` left functionally unchanged (runs through its own `vt100` usage) — step 3 will cut the HTTP endpoint over to the new core; factoring `atn-replay` onto it is a follow-up if output ever diverges. cargo test + clippy + doc warning-free.
- Ops-polish saga step 1 (shell-escape for coordinator commands): fixes the `(priority: High)` bash-syntax-error known-issue in `docs/status.md`. New `atn_core::shell::shell_escape` helper wraps a string in single quotes and escapes interior single quotes with the classic `'\''` dance — the result is always a single POSIX shell word even when the input has parens, spaces, `<`, `>`, `$`, `|`, `&`, `;`, `"`, `'`, `\`, backticks, newlines, or unicode. `canned_action_to_bytes` in `atn-pty::writer` now pipes `CannedAction::ReadWiki.page` and `CannedAction::Ack.request_id` through the helper before formatting the `coord read …\n` / `coord ack …\n` lines that hit the PTY. `CoordinatorCommand { command }` and `setup_commands` are left unescaped by design (they're full shell snippets); `HumanText` stays as-is (LLM agents interpret text, not shell). 10 table-driven unit tests in `atn-core` cover the escape helper (empty string, interior quotes, parens+colon, metacharacter passthrough, unicode, the always-single-word invariant). New `atn-pty` integration tests drive a real bash PTY through `CannedAction::ReadWiki { page: "Requests (priority: High)" }` and `CannedAction::Ack { request_id: "REQ-can't-touch-this" }` and assert the output carries no `syntax error` / `unexpected EOF` and does carry `coord: command not found` — i.e. bash parsed the line as a single word. Ops-polish saga begins; the known-issue bullet for this bug is removed from `docs/status.md`. cargo test (workspace), clippy, doc all clean.
- Windowed-UI saga step 6 (persistence, docs, demo): closes the saga. New `localStorage` key **`atn-window-ui-v1`** carries just the windowed-UI state — `layoutMode`, `sortMode`, `selectedId`, and per-agent `windows[id] = { state, pinned, pinnedRect }` (omitting default-state windows to keep the blob small). `saveWindowUiPrefs` / `loadWindowUiPrefs` handle it; `saveLayoutPrefs` now fans out to both keys and `windowSelect` + `setSortMode` trigger saves so selection + sort choice survive a hard refresh. New top-bar **sort selector** (`Name` default / `Recent` = smoothed-heat-desc) is wired into `buildLayoutItems` so Tiled worker order + Carousel ring follow the choice. New `demos/windowed-ui/setup.sh` (1 coord + 3 workers, local-fake-shim default, `ATN_DEMO_REAL=1` opt-in) stands up the Demo 9 topology and prints a five-step pointer at layouts / chrome / keyboard / sort / persistence. New `docs/windowed-ui.md` walkthrough covers TL;DR, three layouts, chrome + pin semantics, top bar, Option-C keyboard, sparkline row, `atn-window-ui-v1` schema. `docs/demos-scripts.md` gains **Demo 9 "Windowed UI"** in the index + a full section; Demos 6 and 8 get `(legacy model)` banners pointing at Demo 9. `docs/scale-ui.md` banner at the top notes it's the legacy walkthrough and links to `windowed-ui.md`. `docs/usage.md` Dashboard section rewritten around windowed UI; links `docs/windowed-ui.md` first in the See-also list. `docs/status.md` rolls forward: **Current State** now reads "Windowed-UI Saga Complete" and W1..W6 rows (tiled-foundation, window-chrome, stack-layout, carousel-layout, keyboard-option-C, persistence-docs-demo) join S1..S8. Live smoke: 4-agent demo, state changes (stack + coord-pinned-at-rect + sort=recent) round-trip cleanly through a hard refresh; pinned rect `{143, 40, 1113, 645}` restored exactly, UI reflects pinned class + sparkline amber outline + dock population. `cargo doc --workspace --no-deps` warning-free; cargo test + clippy clean.
- Windowed-UI saga step 5 (Option-C keyboard): bare-key window-manager bindings that fire only when focus isn't in an input/textarea/xterm. Replaced the legacy scale-UI keyboard handler (1..9 hottest-unpinned, 0 coord, f focus-size, p pin-focused, `/`, Esc clear-focus) with the selected-window model: `m` minimize selected, `M` (Shift-m) maximize/restore toggle, `p` pin/unpin, `←/→` cycle focus prev/next by sort order (excluding minimized), `1..9` jump to Nth in sort order, `/` focuses the filter input, `Esc` restores any maximized window on first press then deselects on second. Extra `isXtermFocused()` guard checks `document.activeElement.closest('.xterm')` so xterm's own textarea doesn't leak bindings through. Helpers: `keyboardCycleItems`, `keyboardJump`, `keyboardCycle`. Top-bar `#kbd-hint` strip shows the bindings as muted text next to the carousel nav. Per-panel `.pty-focus-badge` ("typing to PTY", amber pill near Send) toggles via a `focusin`/`focusout` document listener that tracks whether the active element is inside an `.xterm`, adding `.pty-focused` to the owning `.agent-panel`. Live browser smoke (3-agent demo): click coord header → `m` minimizes it (moves to dock); select hlasm → `M` maximizes to 1113px (~80%); `Esc` restores, second `Esc` deselects; `2` jumps to the 2nd cycle item; `9` is a no-op with only 2 items; `←`/`→` wrap; `p` pins at the current rect; focusing the xterm textarea then pressing `m` is correctly ignored (selection unchanged, no minimize). cargo test + clippy clean.
- Windowed-UI saga step 4 (carousel layout): third layout mode with a focused primary at ~80% of viewport + prev/next peeks (~9%) on either side, plus `[◀]` / `[▶]` cycle buttons in the top bar. `computeLayout` gains a carousel branch that filters out manually-minimized agents (they stay in the bottom dock, deliverable 5) and builds a name-ordered ring: focus defaults to `selectedId` → coordinator → first, prev/next wrap around the ends, and n≤2 collapses to just the primary or primary+one peek. `setLayoutMode` now accepts `'carousel'` (leaves stack mode when coming from stack, clears any lingering maximized state so carousel/stack math isn't short-circuited). New `carouselCycle(delta)` moves `selectedId` through the eligible ring; `.carousel-btn[data-carousel]` buttons wire prev/next. `windowSelect` re-runs the layout when carousel is active so clicking a header snaps that window to the primary slot. Compact-tile `swap-to-focus` handler also updates `selectedId` so clicking a peek (rendered compact at 9% width) promotes it. Dock-cell clicks in carousel restore + focus. `#carousel-nav` toggles via `updateCarouselNav`. Live browser smoke (3-agent demo): Tiled → Carousel lays out coord primary (1126px of 1400), worker-rpg left peek (125px), worker-hlasm right peek (125px); `◀/▶` cycle shuffles name-order ring; peek click jumps to center; manually minimized worker-hlasm goes to dock and is skipped by cycle; dock-click restores + focuses; Tiled toggle back restores 3-tile grid; persistence round-trips. cargo test + clippy clean.
- Windowed-UI saga step 3 (stack layout): new `Tiled` / `Stack` toggle in the top bar (`.layout-selector`). Switching to Stack reconciles per-window state via `enterStackMode` — one primary (state: `normal`, derived from `selectedId` → coordinator role → first by sort) and everyone else dock-minimized; `computeLayout` short-circuits on `layoutMode === 'stack'` to place the primary centered at ~80% × 90% of viewport and emit the rest as implicit dock entries. Clicking a dock cell in Stack mode routes to new `stackPromote` (swap which window is normal vs minimized + select the clicked); dock-cell clicks in Tiled still restore as before. Switching back to Tiled runs `leaveStackMode` and restores every non-pinned window to `normal`. New agents added while Stack is active auto-join minimized via `addAgentPanel`. Startup re-runs `enterStackMode` if the persisted `layoutMode` is `stack` so refreshes render consistently. Helpers: `pickStackPrimary`, `setLayoutMode`, `updateLayoutSelector`, `setupLayoutSelector`. Live browser smoke (3-agent demo): Tiled → Stack yields 1 normal / 2 minimized with coord primary; dock-click swaps primary to worker-hlasm; Tiled restores 3 normals; persistence round-trips via `atn-scale-ui-v1`. cargo test + clippy clean.
- Windowed-UI saga step 2 (window chrome): each panel header now carries six icon buttons (`_` minimize / `□` maximize-restore toggle / `📌` pin / `▸` config / `↻` reconnect / `✕` delete). `layoutState.windows[id] = { state: 'normal'|'minimized'|'maximized', pinned }` tracks per-window state (plus `layoutState.selectedId` for click-select). Clicking a header outlines that window in accent green (the "selected" window — where step-5 bare-key shortcuts will act). Clicking a chrome icon fires its action without selecting. Minimized windows move off-screen; a new `#window-dock` strip along the bottom of the dashboard renders a 180×48 cell per minimized agent (role + name + state badge + last-line). Clicking a dock cell restores that agent. Maximize lifts one agent to ~80% of viewport centered; all others are implicitly dock-minimized for the duration (their stored state stays unchanged). `windowSelect` / `windowMinimize` / `windowMaximizeToggle` / `windowRestore` plus `reconnectAgent` + `confirmDelete` handlers wire the chrome to the state map. Dock rendering lives in `renderWindowDock`; `escapeHTML` helper used throughout. 60 workspace tests still green; clippy clean.
- Windowed-UI saga step 1 (tiled foundation): replaced the heat-driven treemap with a simple, stable tiled layout. New `computeLayout` places the coordinator in a prominent left slice (55% of viewport width) and grids the remaining workers in name order at equal size on the right. No heat-based sizing, no squarify sort-flipping, no scale hysteresis — Refresh is genuinely idempotent. `applyLayout` now takes a `{windows: [...]}` shape (was `{focus, tiles, groups}`). `buildLayoutItems` sorts by name. `pickTermScale` drops the `currentScale` hysteresis param (no longer needed once tile sizes are stable). `layoutState` gains `layoutMode: 'tiled'` and `sortMode: 'name'` fields (persisted in `atn-scale-ui-v1`; later saga steps introduce stack + carousel + recent-sort). Dead-but-unused treemap code (`squarify`, `groupedSquarify`, `autoPickFocus`, `FOCUS_WIDTHS`) is left in place for later cleanup in step 6.
- Dialog polish for the coordinator-once / workers-many flow. When `transport=local`, the user/@/host group is **hidden** (not just disabled) in both the New Agent dialog and the per-agent Config editor's spec mode — the coordinator create path shows just name + role + transport + dir + project + agent + args. `readSpawnSpec` and `readSpecFromEditor` force `user: null, host: null` for local transport so hidden-input residue can't leak into the composed command. New localStorage key `atn-last-host` remembers the host after any successful POST /api/agents with mosh/ssh transport; when the next dialog open flips transport to non-local, the host input is pre-filled from that value (user, dir, agent stay blank — they differ per worker). + New Agent button now focuses the name input so the flow is keyboard-first.
- Config editor round-trips SpawnSpec. `UpdateAgentBody` on `PUT /api/agents/{id}` gains an optional `spec: Option<SpawnSpec>`. When present, the server validates (400 with missing-field list on fail), recomposes `launch_command` from `spec.compose_command()`, replaces `state.agent_specs[id]`, derives `repo_path` (local → working_dir, remote → base_dir) and role, and runs the usual shutdown + respawn. When absent, the legacy flat overlay (`name/repo_path/role/setup_commands/launch_command`) is unchanged and the stored spec is preserved. The per-agent Config editor in the static dashboard now detects `agent.spec` in the list response and renders a structured editor (role/transport/user/host/working-dir/project/agent/agent_args + live composed-command preview + missing-fields inline error); legacy TOML-loaded agents continue to show the flat editor. `applyConfig` dispatches the right body shape for each mode. Live smoke: mosh agent → PUT `{spec:{...}}` changing working_dir/agent/agent_args → GET shows the new spec + matching composed launch; PUT `{launch_command:...}` legacy shape overrides launch without touching the stored spec; PUT invalid spec returns 400 with `missing: host, user`. No Rust tests added; 60 workspace tests still green; clippy --all-targets clean.
- Scale-UI step 8: 21-agent fleet demo + walkthrough. New `tools/fake-agent-profile` shell shim accepts a profile name as argv[1] and behaves accordingly: `spammer` (line every 100 ms), `quiet` (banner then cats stdin), `periodic` (10 s active / 30 s silent), `awaiting-input` (prints `(y/n) ` and blocks — triggers the state-boost), `error` (5 s of work then exits 1 → Disconnected). `demos/scale/setup.sh` POSTs 21 agents in one go (1 coordinator + 4 spammers + 8 quiet + 4 periodic + 2 awaiting + 2 error) via templated JSON in-script, respecting `ATN_DEMO_URL` and `ATN_DEMO_SKIP_BOOT`. New `docs/scale-ui.md` walkthrough (boot, 8 guided exercises — click-focus+freeze, pin row, 1-9 keyboard, `/` filter, chip interactions, group-by, saved layouts, disconnect visibility, performance notes, teardown). `docs/usage.md` Dashboard section rewritten around treemap + focus + pins + filter; `docs/demos-scripts.md` gains Demo 8 (scale-UI fleet) in the index + picking-a-slot guide + See-also list; `docs/status.md` S7 and S8 rows; README docs index adds demos-scripts + scale-ui. Live smoke confirmed 21/21 agents spawn via setup.sh against an empty server. Closes the scale-UI saga.
- Scale-UI step 7: search, filter chips, group-by, saved layouts. New `.filter-bar` between the view-tabs header and the pin row: `/`-focused text input (`#filter-input`, existing `/` keyboard shortcut targets it), chip toggles for `transport:{local|mosh|ssh}`, `role:{coordinator|worker|qa}`, and `state:{awaiting_human_input|error|disconnected}` (OR within a category, AND across), a `group-by` select (none | role | project), and a Layouts dropdown with Save/Apply/Delete. `matchesFilter(id)` fuzzy-matches text across name / project / role / spec.host / spec.user / spec.agent; `buildLayoutItems` hides non-matching non-pinned agents from the treemap. `computeLayout` honors `groupBy`: `groupItems` partitions filtered items, `groupedSquarify` splits the rest-rect along the longer axis by group heat weights and squarifies each sub-rect; `.group-band` overlay with a dashed border and top-left label surrounds each group. Pin cells always render regardless of filter but get a muted `(filtered)` suffix when excluded via a new `.pin-cell.filtered` style + `syncPinFilteredLabels` called from `applyLayout`. `pollStates` now caches the full `AgentInfo` on each `agents` map entry (`entry.info`) so the filter reads `spec.host` / `spec.user` / role / state without a second fetch. localStorage `atn-scale-ui-v1` gained `filterText` / `filterChips` / `groupBy`; new `atn-scale-ui-saved-layouts-v1` holds named layout snapshots ({pins, focusId, focusSize, filterText, filterChips, groupBy}). Filter-bar visibility tied to agent count (hidden on empty start). 60 workspace tests green; clippy --all-targets clean.
- Demo scripts doc: new `docs/demos-scripts.md` curates every demo the current build supports — empty-start + New Agent dialog (local + mosh/ssh preview), three-agent topology (fake shims + real CLIs), reconnect after mosh drop, graceful delete clearing remote tmux, treemap scale-UI (heat / focus / pin / keyboard / persistence), REST API tour. At-a-glance index with duration + infra prereqs; cross-linked from usage.md, uber-use-case.md, demo-three-agent.md; status.md gains S1–S5 and S6 rows.
- Scale-UI step 5: pin row, keyboard shortcuts, persistence. New `.pin-row` strip above the dashboard renders mini compact-tiles for user-pinned agents (fixed 180×82, sparkline + last-line + state badge, × button to unpin). Pinned agents are excluded from the heat-sized treemap and from focus auto-pick; pinning the current focus returns focus to auto-pick. `.btn-pin` pin button on every agent-header (clicking sets `.pinned` style). Keyboard: `1–9` focus Nth hottest unpinned, `0` focus coordinator, `f` toggles focus panel between medium (40–50%) and large (55–70%) widths, `p` pin/unpin current focus, `/` focuses filter input (stub until step 6), `Esc` clears focus → auto-pick. `isTypingTarget` guard ignores keys inside inputs/textareas/selects/xterm. localStorage key `atn-scale-ui-v1` persists `{ pins, focusId, focusSize }`; hydrated at init before the first render. Existing `.agent-panel` click listener now honors `[data-no-focus]` so clicking the pin button doesn't also swap focus. 60 workspace tests green; clippy --all-targets clean.
- Treemap layout: `.dashboard.treemap-mode` replaces the cols-1..cols-4 grid with an absolutely-positioned canvas. New `squarify(rect, items)` classic Bruls/Huijsen/van Wijk squarified treemap (with an on-load self-test covering area-fill, bounds, single-item, empty). `layoutState` carries `focusId` + `freezeUntil` + a per-agent smoothed-heat EWMA (~5s half-life at 1 Hz; keeps tiles from jittering on byte bursts). `computeLayout` carves out a focus panel (40–55% of viewport width, scales with relative heat) and runs squarify on the rest. `applyLayout` places panels via absolute `left/top/width/height`, flips each tile between xterm and compact mode based on `pickTermScale`, and follows with `updateTileScales`. `swap-to-focus` CustomEvent is now handled end-to-end: sets `focusId`, freezes layout for 5s, calls `setFocus` for the PTY+xterm fit. Clicks on `.agent-header` and compact tiles both dispatch it. Refresh tick runs every 1.5s unless frozen. Auto-pick prefers a coordinator; falls back to highest-heat overall. 60 workspace tests green; clippy clean.
- CSS-scaled xterm: every panel nests `.agent-terminal > .term-scale > xterm-host`. xterm is created at fixed native geometry (`TERM_NATIVE_COLS=120`, `TERM_NATIVE_ROWS=40`); tile size tracking happens via `transform: scale(var(--term-k, 1))` on the wrapper. `pickTermScale(tileW, tileH, natW, natH)` snaps to `{0.25, 0.33, 0.5, 0.66, 0.8, 1.0}` or returns null below threshold (→ compact tile). `updateTileScales` iterates every xterm panel on window resize / ResizeObserver / grid-class change and updates the CSS var — no `fitAddon.fit()`, no `POST /api/agents/{id}/resize`. Only `setFocus(id)` (dev-exposed as `window.setFocus`) still fits + syncs PTY size, for the single focused tile. Inline self-test (`testPickTermScale`) runs on load and logs PASS/FAIL for 8 cases.
- Compact tile: small-footprint agent tile variant with no xterm inside — header (name/role/state badge) + body (inline SVG sparkline, truncated last-line, click-to-focus hint). SSE output pipeline now also feeds a per-agent last-line buffer (ANSI-scrubbed, ≤200 chars); a 1Hz poll of `/api/agents/heat` fills a 60-sample `bytes_per_sec` ring per agent that the sparkline reads. Clicking a compact tile dispatches a `swap-to-focus` CustomEvent (step 4 will wire the layout side; the default handler just logs). `window.toggleCompactMode(id)` from devtools swaps between xterm and compact for manual testing before the step-4 auto-pick. Supporting helpers: `ensureAgentMeta`, `recordOutputBytes`, `pollHeat`/`startHeatPolling`, `sparklinePath`, `createCompactTile`, `refreshCompactIfPresent`. 60 workspace tests still green; clippy `--all-targets` clean.
- Heat score: new `atn-server/src/heat.rs` tracks a per-agent EWMA of output bytes/sec (time-aware; long quiet gaps decay properly) plus state multipliers (awaiting_input / error / blocked get boosted, disconnected gets muted) and a floor so cold agents keep a visible tile. `spawn_heat_tracker` subscribes to the PTY output broadcast, feeds byte counts into a shared `HeatMap`, and cleans up on `Disconnected` / channel close. Wired into every spawn path (startup loader, create, restart, reconnect, hot-reload); removed on delete and hot-reload removal via a small `rearm_heat_tracker` helper. `GET /api/agents/heat` decays each entry to the current instant before returning `{id, heat, bytes_per_sec, state_boost}` per live agent. 6 unit tests cover EWMA bursts/decay, state-boost layering, floor, clamping. Live smoke verified heat rises on spawn, decays over silence, climbs again when inputs pump output, and drops on delete.
- Hot-reload keeps `agent_specs` in sync with `agents.toml`. `spawn_config_watcher` previously only updated `agent_configs` + `agent_repo_paths` on reload, so external edits that added or removed `[agent.spec]` subtables left the runtime spec map stale (UI's structured view stayed blank, Save-to-TOML dropped the spec). The watcher now inserts a clone of each entry's spec on add/update, removes stale specs when an entry loses its `spec` field, and drops specs for agents removed from the config entirely.
- Save-to-TOML round-trips the full SpawnSpec. `AgentEntry` gets an optional `[agent.spec]` subtable; when present, `to_agent_config` recomposes the shell command from the spec (so `launch_command` stays blank on disk and can't drift). Startup loader populates `state.agent_specs` from any entries with spec fields; `save_config` writes the spec subtable whenever a spec is known. Two new round-trip tests in atn-core (spec_roundtrips_through_toml, entries_without_spec_omit_the_field_when_serialized); live smoke confirmed that a mosh agent created via the dialog writes a clean spec subtable on Save.
- Agent panel now surfaces the composed launch command: hover the agent name for the full shell line (including `mosh user@host -- tmux new-session -A -s atn-<name> 'cd DIR && AGENT'` for remote agents), plus a small monospace line beneath the header shows a truncated version at a glance. Makes the demo self-documenting — no need to hit the API or open the Config editor to see what ATN is actually running.
- Reconnect button in the static dashboard: closes the step-3 UX gap — the `/api/agents/{id}/reconnect` endpoint (hard-kill local mosh/ssh + respawn → re-attach to the remote tmux session) is now one click instead of a curl. Button sits next to Restart with its own green styling; click handler closes the SSE stream, POSTs the endpoint, reconnects after 1s, and shows transient "Reconnecting…/Reconnected/Failed" labels. Terminal scrollback is preserved on reconnect (unlike Restart which clears it).
- Docs refresh closes the remote-agent saga: `docs/usage.md` rewritten around empty-start + the New Agent dialog (SpawnSpec fields, transport templates, lifecycle endpoints incl. /reconnect, `ATN_PORT`); new `docs/demo-three-agent.md` walks the CI and live paths; `docs/uber-use-case.md` implementation notes now reflect what actually shipped and cross-link usage/demo/remote-pty; `docs/status.md` adds R1–R5 rows; README gets a quickstart that points at the demo. `cargo doc --workspace --no-deps` builds warning-free.
- Three-agent demo: `ATN_PORT` env var (and the machine-readable `atn-server ready on <addr>` stdout marker) let integration tests bind ephemeral ports; three fake-agent shim scripts (`tools/fake-claude`, `tools/fake-codex`, `tools/fake-opencode-glm5`) stand in for the real CLIs; `demos/three-agent/fixtures/*.json` capture the real topology (coordinator/local + two queenbee workers via mosh) and `demos/three-agent/setup.sh` boots ATN (or reuses a running one), POSTs each fixture, and toggles fake↔real via `ATN_DEMO_REAL=1`; new `crates/atn-server/tests/three_agent_demo.rs` end-to-end test spawns the real binary, POSTs three CI-variant specs, waits for non-`starting` states, routes coordinator→worker-hlasm *and* coordinator→worker-rpg events, and asserts both reach their inboxes and show up in `/api/events`.
- Remote PTY transport: PTY exit detection — `OutputSignal::Disconnected` emitted by the reader on EOF/read-error, picked up by the state tracker to flip `AgentState::Disconnected`; `tools/fake-mosh` records argv to `$ATN_FAKE_MOSH_LOG` and is symlinked as `mosh`/`ssh` for tests; integration tests assert composed argv byte-for-byte for both transports and verify exit-→-Disconnected; new `POST /api/agents/{id}/reconnect` hard-kills the local mosh/ssh child without sending Ctrl-C and respawns (re-attaches to the remote tmux session via `tmux new-session -A -s atn-<name>`); `DELETE /api/agents/{id}` now sends `C-b :kill-session <Enter>` over the PTY for remote agents before shutdown so the remote tmux session is cleaned up; added `docs/remote-pty.md` with the manual queenbee walkthrough.
- New-agent dialog: structured spawn replaces the free-form launch field. `atn-core/spawn_spec::SpawnSpec` captures name/role/transport/host/user/working_dir/project/agent/agent_args, validates per-transport, and composes the shell command (local: `cd DIR && AGENT`; mosh/ssh: `TRANS USER@HOST -- tmux new-session -A -s atn-NAME 'cd DIR && AGENT'`). `POST /api/agents` consumes the new shape, returns the created `AgentInfo` with `spec` + `launch_command`, and 400s with a `missing` field list. Static dashboard and Yew UI both got the matching multi-field dialog with live command preview; Yew gets a real modal replacing the step-1 stub. 13 new unit tests for composition/validation; live end-to-end smoke verified.
- Empty-start: ATN boots cleanly with zero agents (`agents.toml` now empty; seed preserved as `agents.example.toml`); `ProjectConfig.agents` gets `#[serde(default)]`; Yew UI and static dashboard render a dedicated empty state with a "+ New Agent" CTA; new integration test `empty_start_loads_zero_agents`; kicks off a new saga targeting the remote-agent demo (mighty-mike/claude + queenbee/devh1/codex + queenbee/devr1/opencode-z-ai-glm-5); adds `docs/uber-use-case.md`
- Standalone wiki HTML UI: browsable wiki at /wiki with sidebar navigation, edit/create pages, classic wiki-links ([[Page Name]]) with red links for missing pages that open create forms on click, "Open in Wiki" link from the ATN Wiki tab; agent list sorting (coordinator always first in all views); "Open in Wiki" link in ATN tab toolbar
- Phase 9: Interactive TUI agents + multi-saga UI + agent-to-agent delegation — PTY resize sync (browser↔PTY), TERM=xterm-256color for TUI apps, \r instead of \n for Enter in TUI mode, raw keystroke forwarding from xterm.js (arrows/Tab/Escape), raw_bytes input API, per-agent saga panels side-by-side in Saga tab, router delivers events as TUI prompts (not bash comments), outbox path fix for remote base_dir, submit_event uses base_dir
- atn-replay crate: Rust CLI for rendering PTY transcript.log files; screenshot (--html standalone, --html-fragment for org embed), at (offset), steps (chunked), dashboard (org-mode for emacs auto-revert), list, text (plain)
- Input logging: writer task logs all InputEvent to inputs.jsonl with RFC3339 timestamps for correlation with transcript.log
- Demo: 2-agent app-building scenario — dev creates a CLI app with "greet" command, ATN routes feature request to second agent who adds "farewell" command, both verified; reg-rs regression test; docs/demo-review.org with PTY screen captures; docs/needed-tools.md
- Phase 8: Session management UI — agent CRUD REST endpoints (POST/PUT/DELETE /api/agents), stop endpoint, save-to-TOML endpoint, "New Agent" form in browser, per-agent stop/delete/config-edit controls, dynamic dashboard refresh on create/destroy
- Phase 7: Polish and robustness — graceful SIGINT/SIGTERM shutdown, agent restart endpoint + UI button, dependency graph visualization (Graph tab with SVG), notification sounds via Web Audio API, agents.toml hot-reload with file watcher, panic hook + structured logging, documentation updates
- Phase 6: Agentrail integration with Saga tab UI, per-agent saga badges, step progress timeline, trajectory viewer, skill distill button, REST API (GET /api/saga, GET /api/agents/{id}/saga, POST /api/saga/distill)
- Phase 5: Message routing with outbox polling, PushEvent routing (deliver/escalate/broadcast), event log REST API, Events tab in UI
- Phase 4: Wiki integration with REST API (GET/PUT/PATCH/DELETE), ETag CAS, browser UI in both static HTML and Yew
- Phase 3: Multi-agent management with dashboard grid and state tracking
- Phase 2: Minimal web UI with SSE terminal streaming
- Phase 0+1: Workspace skeleton with PTY integration tests
